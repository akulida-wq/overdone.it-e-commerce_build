# PROMPT 4 — Сравнение моделей, «Без початкового бюджету», «Що ми беремо на себе»

Скопируй в Claude Code:

---

Прочитай CLAUDE.md и docs/PAGE_SPEC.md §7–§9. Этап 4 из 7. Сделай:

1. `SectionCompare.vue` по §7: два блока один под другим (классика / партнёрская модель), 5 строк «критерий — значение» из `compare.*`, второй блок с красным glow-бордером и бейджем «Наша модель». Hover-связь одинаковых строк между блоками (по index). Появление строк парами
2. `SectionModel.vue` по §8: текст + декоративный SVG-график (плоская muted-линия оплаты до отметки «Запуск», после — растущая белая линия продаж и красная линия процента), отрисовка stroke-dashoffset при входе в вьюпорт, подписи только из локалей, никаких цифр на осях
3. `SectionScope.vue` по §9: 7 строк-аккордеонов с номерами 01–07, hover-раскрытие на desktop + клик-фиксация, только клик на mobile (первый пункт открыт), покраснение номера, сдвиг строки, reveal сверху вниз
4. Подключи в index.vue

Закончи: `npm run build`. Проверь: hover нигде не единственный способ открыть контент (клавиатура и тач работают), высоты аккордеонов анимируются без дёрганья, обе локали, 1440/768/390, консоль чистая.

Если что-то пошло не так: анимация высоты — через grid-template-rows 0fr→1fr или GSAP height auto, не через max-height «с запасом»; если hover-связь блоков сравнения тормозит — переключай класс через один обработчик на контейнере (event delegation).

---
