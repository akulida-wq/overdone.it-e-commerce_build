# PROMPT_R2 — промты круга правок 2

Прогонять по порядку, каждый — отдельной задачей. Перед кругом положи в папку проекта фото `r2-s1`…`r2-s18` (уже там) и файл `CHANGES_R2.md` (в prompts/). Все промты требуют смотреть фото — не пропускай это, результат сверяется с ними.

---

## R2-P1 — Лого, стекло, ползунки

Прочитай CLAUDE.md и prompts/CHANGES_R2.md полностью. Найди в проекте фото-референсы Glob-ом `**/r2-s*` — понадобятся r2-s3, r2-s10. Выполни пункты R2.1 (лого с поворотом красного круга), R2.11 (глобальный миксин glass + применение к перечисленным карточкам/формам с fallback и проверкой производительности) и R2.3 (редизайн PSlider по фото r2-s3 — посмотри фото до начала). Проверь ползунки в калькуляторе и в форме: drag мышью, тач, клавиатура, подпись значения следует за ручкой. `npm run build` до чистого билда, обе локали, консоль чистая.
Если что-то пошло не так: если backdrop-filter лагает при скролле — снижай blur до 8px, на pin-секциях отключай на время скраба; если у лого нет SVG-исходника — не конвертируй растр, отчитайся.

## R2-P2 — Hero и What we handle

Прочитай prompts/CHANGES_R2.md §R2.7 и §R2.2. Посмотри фото r2-s12 (реф hero), r2-s1 и r2-s2 (scope: текущее/цель). Открой рефы https://spur.us и https://thewatch.60fps.fr — если контент не отдаётся, работай по фото и описаниям фаз в CHANGES. Выполни:
1. R2.7 — пересборка hero: центрированная композиция как у spur, новая короткая строка-суб из CHANGES в обе локали, старые абзацы не выводить
2. R2.2 — переработка SectionScope: цифра слева по центру оси заголовка, плюс справа, описание по центру на hover; активная строка через elementFromPoint на событиях mousemove + скролла Lenis (НЕ через CSS :hover); появление строк из-под маски снизу-вверх как на thewatch
Проверь руками: наведи мышь на список и скролль колесом не двигая мышь — активная строка должна меняться под курсором. `npm run build`, обе локали, 1440/768/390, reduced-motion, консоль чистая.
Если что-то пошло не так: elementFromPoint зови не чаще raf и только при движении/скролле; если строки «моргают» на границе — переключай активную только при смене найденного row.

## R2-P3 — Слои (neoconda)

Прочитай prompts/CHANGES_R2.md §R2.4. Посмотри фото r2-s4 (текущее), r2-s5, r2-s6, r2-s7 (реф: закрыто → раскрывается → раскрыто с подписями). Открой https://neoconda.com. Переработай SectionLayers:
1. Сначала статично: новая шапка секции (заголовок слева, описание справа), крупная центральная сцена; нарисуй детальные слои — на каждом своя мини-графика по смыслу (список в CHANGES), стиль stroke 1.5px + surface
2. Потом scrub-раскрытие по фазам с фото s5→s6→s7
3. Подписи лесенкой со сменой сторон (1 — слева сверху, 2 — ниже справа, 3 — ниже слева…), линии-выноски, появление opacity 0→1 по мере раскрытия своего слоя
4. Mobile/reduced-motion: статичное раскрытое состояние, подписи списком
Сверь результат с фото s7 — покадрово похоже? `npm run build`, обе локали, консоль чистая, скролл плавный (transform-only).
Если что-то пошло не так: мерцание слоёв — backface-visibility hidden + translateZ(0); если сцена тяжёлая — упрощай мини-графику слоёв, а не выкидывай её.

## R2-P4 — Included (produx) и Two ways to build (тарифы)

Прочитай prompts/CHANGES_R2.md §R2.5 и §R2.9. Посмотри фото r2-s8, r2-s9, r2-s10, r2-s15. Открой https://www.produx.design (блок Trusted partners) и https://orionix.framer.website/pricing. Выполни:
1. R2.5 — SectionIncluded: новые title/lead из CHANGES в обе локали (заголовок слева, описание справа), квадратные стеклянные карточки крупнее с более детальными иконками, последовательная автоподсветка карточек + красный прогресс-бар как на produx, hover перехватывает подсветку
2. R2.9 — SectionCompare: две горизонтальные карточки-«тарифа» по фото r2-s15, наша модель акцентная с бейджем и CTA → #contact, иконки пунктов (нейтральные vs красные галочки), mobile — наша карточка первой
`npm run build`, обе локали (длинные укр. строки в карточках), 1440/768/390, консоль чистая.
Если что-то пошло не так: автоподсветка — один interval/таймлайн на секцию с паузой при hover и вне вьюпорта (IntersectionObserver), не плоди таймеры.

## R2-P5 — Проекты (trionn) + страница кейса Paparazzi

Прочитай prompts/CHANGES_R2.md §R2.8. Посмотри фото r2-s13 и r2-s14. Открой https://trionn.com (блок проектов), https://www.produx.design/projects/jurniAI, https://mesh3d.gallery/blog/mesh3d-render-014. Выполни по порядку:
1. Скачай og:image со страницы https://pprz-school.overdone.it/ → public/img/partners/paparazzi-og.webp
2. Сгенерируй обложку «Ваш продукт може бути наступним» кодом (тёмный фон, красное свечение, крупный +, пунктирная сетка) → webp
3. Переработай SectionProjects под trionn: крупные карточки-обложки, hover-scale картинки внутри рамки, анимация появления; Paparazzi ведёт на /projects/paparazzi, вторая карточка — на #contact
4. Установи playwright (chromium), выясни механизм тёмной темы на pprz-school.overdone.it (класс html / localStorage) и сними в тёмной теме 1440×900: главную, каталог с фильтрами, страницу товара → public/img/case-paparazzi/*.webp. Если тема не включается программно — сними как есть и пометь TODO(USER)
5. Собери pages/projects/paparazzi.vue по структуре из CHANGES: hero кейса, блок «что сделали», скрины в браузерных рамках с подписями (тексты case_pprz.* из CHANGES в обе локали), два плейсхолдера под скрины админки с TODO(USER), CTA внизу. Шапка/футер/язык работают на странице кейса
`npm run build`, прокликай переходы карточка→кейс→CTA→форма, обе локали, консоль чистая.
Если что-то пошло не так: og:image может быть jpg/png — конвертируй в webp через sharp; playwright в CI-режиме — `--no-sandbox` не добавляй без необходимости, жди networkidle перед скрином.

## R2-P6 — Steps (daoism), Align (sharplink), Model (trionn about), FAQ

Прочитай prompts/CHANGES_R2.md §R2.10, §R2.12, §R2.13, §R2.6. Посмотри фото r2-s16, r2-s17, r2-s18, r2-s11. Открой https://www.daoism.systems (network of trust), https://www.sharplink.com (Pioneering Productivity и FAQ), https://trionn.com (about). Выполни:
1. R2.10 — SectionSteps: pin, слева стопка стеклянных карточек шагов с перелистыванием по scrub (назад тоже отматывается), справа синхронный текст шага, прогресс-точки; mobile/reduced-motion — вертикальный список
2. R2.12 — SectionAlign по фото r2-s17 один в один в наших цветах: сетка с line-art векторами под наши 4 смысла (щит/пульс, чек-поток, автоматизация, корзина); старую колоду карточек удалить
3. R2.13 — SectionModel: заменить на короткий statement из CHANGES (обе локали), пословное проявление из muted в белый по scrub как trionn about, ключевая фраза красным, график удалить; reduced-motion — текст сразу белый
4. R2.6 — FAQ: стеклянные плашки на всю ширину по фото r2-s11, зазор 12–16px, плюс в круге, открытая плашка с бордером strong
Финал круга: `npm run build`, полный проход страницы сверху вниз в обеих локалях на 1440/1024/768/390 + reduced-motion, проверь что две pin-секции (Layers и Steps) не конфликтуют (refresh при resize), консоль чистая. Выпиши все оставшиеся TODO.
Если что-то пошло не так: рассинхрон текста и карточек в Steps — один общий timeline, оба привязаны к его прогрессу; пословная анимация — SplitText не используем (платный), дели текст на span-ы своей функцией с сохранением пробелов и aria-label исходной строкой на контейнере.
