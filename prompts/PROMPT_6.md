# PROMPT 6 — FAQ и форма

Скопируй в Claude Code:

---

Прочитай CLAUDE.md, docs/PAGE_SPEC.md §13–§15 и CONTENT_ADDITIONS §6. Этап 6 из 7. Сделай:

1. `SectionFaq.vue` по §13: 7 вопросов из `faq.*` на PAccordionItem, открыт максимум один, aria-expanded, клавиатура
2. `PInput.vue`, `PChips.vue` (мультивыбор) в ui/ по стилю DESIGN §4
3. `SectionContact.vue` (id="contact") по §14:
   - слева текст + кнопка Telegram
   - справа форма с двумя табами `form.tab_has` / `form.tab_no`; поля по спеке (общие: имя*, компания, контакт* с hint; таб A: ссылка*, что продаёте, объём PSlider, обработка заказов чипами; таб B: что планируете*, каналы чипами, ожидаемый объём). Переключение табов без прыжка высоты
   - валидация on-blur + on-submit, тексты ошибок из локалей, ошибка под полем
   - submit: loading на кнопке, mock-отправка 800ms с `// TODO(API): endpoint`, success-состояние вместо формы (`form.success_*` + Telegram)
   - фон секции: красное свечение снизу + слот TODO(ASSET) под 3D-фигуру
4. Финализируй PFooter §15 (если что-то осталось заглушкой): колонки, реквизиты, свитчер языка, Back to top
5. Проверь всю цепочку CTA: hero-кнопка, карточка «Ваш продукт…», шапочный «Контакт» — все плавно скроллят к #contact

Закончи: `npm run build`. Проверь: обе локали (длинные укр. label не рвут сетку формы), валидация не даёт отправить пустые обязательные поля, success-состояние показывается и на A и на B табе, тач-таргеты ≥44px, автозаполнение браузера не ломает стили (autofill), консоль чистая.

Если что-то пошло не так: прыжок высоты при смене таба — задай min-height контейнера по большему табу через measure после fonts.ready; стили autofill — переопредели -webkit-autofill box-shadow под цвет фона.

---
