# PROMPT 5 — Шаги партнёрства, калькулятор, «Ми заробляємо…»

Скопируй в Claude Code (Opus):

---

Прочитай CLAUDE.md и docs/PAGE_SPEC.md §10–§12, тексты — CONTENT_SOURCE + CONTENT_ADDITIONS §4–5. Этап 5 из 7. Сделай:

1. `SectionSteps.vue` по §10: desktop — pin со sticky-номером слева (контурный → красный при активации, смена с blur/slide 250ms), справа 5 описаний с активным состоянием, вертикальная линия прогресса по scrub. Не потеряй второй абзац шага 04 про сроки. Mobile/reduced-motion — вертикальная timeline без pin, всё раскрыто
2. `PSlider.vue` — переиспользуемый ползунок на `input type="range"`: ступенчатые значения (snap), стилизация по спеке §11, label + aria-valuetext строкой ступени, событие change
3. `SectionCalc.vue` по §11: слева текст + 9 факторов mono-чипами (4 связанных подсвечиваются при движении своего ползунка), справа карточка с 4 ползунками и результатом «X–Y%» (каунтер 300ms). Формула — эвристика из спеки, оформи отдельной функцией с комментарием TODO(FAKE): это ориентир для вовлечения, не бизнес-логика. Дисклеймер под результатом обязателен, не меньше 12px
4. `SectionAlign.vue` по §12: заголовок, абзац, колода 4 карточек `align.card_1..4` с автоперелистыванием 3.5s, паузой на hover, точками-индикаторами и ручным перелистыванием по клику; замыкающий абзац. Reduced-motion — сетка 2×2
5. Подключи в index.vue

Закончи: `npm run build`. Проверь: ползунки управляются с клавиатуры (стрелки), значения читаются скринридером, каунтер не прыгает по ширине (табличные цифры font-variant-numeric: tabular-nums), pin шагов корректен при resize, обе локали, консоль чистая.

Если что-то пошло не так: если счётчик диапазона мигает — обновляй его через gsap.to с snap, а не через реактивную строку на каждый тик; если карточки колоды «съезжают» — позиционируй абсолютно внутри контейнера с фиксированным aspect-ratio.

---
