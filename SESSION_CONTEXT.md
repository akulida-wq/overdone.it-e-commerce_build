# SESSION_CONTEXT — Overdone E-commerce Partnership Landing

Читать первым в каждой новой сессии. План работ: prompts/PROMPT_1..7.md, правила: CLAUDE.md, спека: docs/.

## Статус: R3…R18 + R19 (2026-08-30) ЗАВЕРШЕНЫ. Ждём следующего просмотра. HANDOFF.md актуален

## R31.4 (2026-09-04) — calc→earn ещё +40 — ГОТОВО
- padding-bottom секции калькулятора: rhythm+40 → rhythm+80 (зазор чипсы→«We earn» 278 @1440)

## R31.3 (2026-09-04) — пилюля по центру, больше воздуха calc→earn и hero→стейтмент — ГОТОВО
- Пилюля хедера на десктопе (≥1025) — АБСОЛЮТНЫЙ центр страницы (left 50% top 50% translate −50/−50): flex-центрирование уезжало влево, когда правый блок (lang+CTA) стал шире лого (заметно на ≥1920); зазоры до соседей ≥89 даже на 1100
- ОТКАТ R31.1: заказчик имел в виду ПРОТИВОПОЛОЖНОЕ — зазор calc→earn теперь rhythm+40 (238 @1440, было 142)
- Стейтмент: ещё +80 сверху (итого rhythm+168, gap hero→стейтмент 423)
- ГРАБЛЯ КОММУНИКАЦИИ: «сделай отступ таким же как…» без стрелки направления — переспрашивать/показывать скрином, R31.1 сделал наоборот

## R31.2 (2026-09-04) — корзина ещё левее, воздух до стейтмента ещё больше — ГОТОВО
- Десктоп-корзина hero: left calc(50% − 2vw) (было −1vw; центр −29px @1440)
- SectionModel padding-top: rhythm + 48 + 40 (итого +88 к базовому ритму; зазор hero→стейтмент 383)

## R31.1 (2026-09-04) — воздух под секцией калькулятора = воздуху над ней — ГОТОВО
- Слак от row-центрирования (min-height 440 против контента ~370) давал снизу 198 против 142 сверху → десктопный padding-bottom секции −56 (raw @media ≥1025) — замер 142/142

## R31 (2026-09-04) — БАГ ПИНА S2, большие экраны 1:1 с оригиналом, воздух над стейтментом — ГОТОВО
- БАГ (скрин заказчика, воспроизведён на проде): после переноса S2 вниз её пин стартовал на ~2600px РАНЬШЕ позиции → секция скроллилась поверх соседей. ДВЕ ПРИЧИНЫ: (1) триггеры S2 создавались синхронно в onMounted, когда pin-спейсера S8 (создаётся в nextTick) ещё нет в DOM — старт посчитан без него; (2) обычный ScrollTrigger.refresh() НЕ пересчитывал старые старты — порядок компенсации спейсеров замораживается порядком СОЗДАНИЯ, лечит только ScrollTrigger.sort() + refresh(true). ФИКС: initPin+entrance С2 в nextTick + в index.vue settled-рефреш (mount+fonts.ready+load+150ms) с sort()+refresh(true). При SSR window 'load' пролетает ДО гидрации — авторефреш ScrollTrigger на load не случается вообще. Проверка: pin start == docTop (11360), транзит колесом чистый. QA-ручка: window.__ST (dev-only)
- БОЛЬШИЕ ЭКРАНЫ (сверка с живым overdone.it на 2000): --container-max 1720→1790 (её кап, контент на 2000 начинается с 105px как у них 1:1), ≥2560 2290→2387 (1790×4/3); у ХЕДЕРА оригинала своя ступень ×4/3 с ≥1920 (замер: h 72→96, CTA 36→48, пилюля 440×62→584×83, лого до 50) при НЕИЗМЕННОЙ типографике до 2560 → новый множитель --hk (1 / 1.3333 @1920 / 1.4 @2560) только для хедера (+ офсеты кейс-страницы); лого капнуто min(41*hk, 50px). Замер 2000: header 96, pill 587×83, CTA 48, contentLeft 105 — всё 1:1
- Стейтмент: +48px воздуха сверху на десктопе (+24 мобилка) — «чтобы визуально как снизу»
- ГРАБЛЯ ГЛОБАЛЬНАЯ: пины, создающиеся в разные такты + SSR = замороженные старые старты; при любых перестановках секций прогонять транзит колесом

## R30 (2026-09-04) — paparazzi.shop живой: ссылка, новые каверы, кейс скрыт — ГОТОВО
- Карточка Paparazzi: ссылка (карточка+кнопка Website) → https://paparazzi.shop (магазин переехал с pprz-school.overdone.it; сам магазин редиректит на /ru-RU — это их сторона)
- Кнопка «Детали» закомментирована; кейс-страница СКРЫТА через routeRules '/projects/**' → redirect '/' в nuxt.config (файл страницы цел, вернуть = убрать правило) — ВАЖНО: смена nuxt.config требует рестарта dev-сервера
- Новые каверы заказчика (Rectangle.jpg=UA, Rectangle-1.jpg=EN с Десктопа) → public/img/partners/paparazzi-cover-{ua,en}.webp (1400×780, sharp q82); в карточке :src по локали (lang из useLang)
- ГРАБЛЯ ПРОБ: у карточки entrance clip-path inset(100%) + ScrollTrigger once 'top 70%' — при мгновенном scrollTo/element-screenshot триггер не стреляет и карточка ЧЁРНАЯ; проверять только честным колесом
- Проверено: каверы рендерятся по локалям (naturalWidth 1400), клик → paparazzi.shop, редирект кейса работает (после рестарта dev), консоль чистая

## R29.2 (2026-09-04) — «Бізнеси, що зростають разом з нами» перед FAQ — ГОТОВО
- SectionProjects (S2, «Businesses growing with us») перенесена 1в1 из слота 3 на предпоследнее место: …Calc, Earn, PROJECTS, Faq, Contact; пин/стретч-механика прогнана колесом на новом месте — цела; порядок проверен
- Прод R29.1 перепроверен со свежим кэшем: markW 559, rightInset 0, cart left 50%−1vw ✓ (первый замер ловил stale edge-кэш Vercel — при проверке прода сразу после деплоя обходить кэш ?v=Date.now())

## R29.1 (2026-09-04) — процент: правый край + центр; корзина hero левее — ГОТОВО
- ГРАБЛЯ: align-self:stretch + aspect-ratio + вложенный height:100% в флексе резолвятся ЦИКЛИЧНО — знак то схлопывался (300), то вылетал за правый край контейнера на широких экранах (скрин заказчика 1999px) → детерминированная цепочка: mark-wrap width 559*k (= 440×427/336) → tilt width 100% → mark 100% → svg; никаких aspect-ratio
- __inner align-items: center — центры процента и левого блока совпадают (замер diff 8px); правый край знака = правая граница контейнера (inset 0)
- Корзина hero на десктопе: left calc(50% − 1vw) — «слегка левее», композиция съезжала вправо (замер центра: −14px @1440, −20 @1999)

## R29 (2026-09-04) — модалка, процент во всю высоту, UA-заголовок одной строкой, табы, воздух — ГОТОВО
- Модалка выбора языка на первый визит УБРАНА (закомментирована в index.vue), дефолт en; свитчеры хедер/футер остаются
- Процент калькулятора: на всю высоту секции (mark-wrap align-self stretch, mark height 100% + aspect-ratio 427/336, у __inner min-height 440*k — иначе full-height давал 402<427, левая колонка всего ~316) → 559×440; тилт: rotateY 13.5→10 (влево/вправо меньше), rotateX 9→12 (вверх/вниз больше)
- UA hero: длинное тире — → короткое – (в целом, ua.json); «Потім – Оплата» = 6.31em ОДНОЙ строкой на 390 → UA 3 строки как EN, --hero-fs: min(64px, 15.85vw−5.1px) (56.7@390, 64 от ~436), --hero-lines: 3; l2 em-кап 6.23→6.5
- Мобильная корзина hero 112vw→120vw (резерв низа 62vw→66.5vw)
- Табы формы ≤480: сегмент-контрол (контейнер r32 + padding 4 + gap 4, margin табов 0) — «поплывший» овал починен
- Аккордеон scope: нижний воздух на мобилке 121→97 (padding-bottom секции → 8 на md)
- Проверки: свежий визит без модалки/en; UA 390: 3 строки, clip 0, корзина 468, gap 24; процент 559×440; консоль чистая; билд ок; задеплоено

## R28 (2026-09-04) — мобильные боковые 16 + межкарточные ≤16 — ГОТОВО
- $container-pad-m 24→16 («стандарт» заказчика); hero-формулы пересчитаны на паддинги 16×2: EN 18.8vw−6px, UA 18.3vw−5.9px — теперь ОБЕ локали держат ровно 64px c 390 (UA дотянулась благодаря ширшему контейнеру)
- Межкарточные зазоры на ≤768 капнуты в 16: лента S4 included-card (был 20), стек compare-card (20), грид step-card (20); НЕ тронуты (композиция/не карточки): earn-таблица с общими бордерами, FAQ-строки, scope-аккордеон, чипсы (8), внутрикарточные отступы
- Матрица 320/390/480/640/768 × EN/UA: rows 3/4, clip 0, gap 24, pad 16, ovf 0; консоль чистая; задеплоено

## R27 (2026-09-04) — большой круг «поженить с оригиналом» (док: ../CHANGES_R4.md, фото r3/) — ГОТОВО
- ХЕДЕР 1:1 с живым overdone.it (замерено headless): h72 (было 88), лого = серое кольцо оригинала 69×50 h41 (LogoOverdone переписан, вращение кольца на ховер сохранено, transform-origin 34.08/24.68), пилюля 440×62 линки 14px в ПОТОКЕ (margin-inline auto = центр между лого и правым блоком, как у оригинала — НЕ центр страницы), CTA компакт 36px/15px (override .p-button в хедере), тикер 29px
- СВИТЧЕР ЯЗЫКА в хедере (Figma 1093:4443): «EN/UA ⌄» серый + дропдаун, useLang.setLang, закрытие по клику вне/Esc; на мобилке НЕТ (решение заказчика — только футер)
- Мобильный хедер = ТОЛЬКО пилюля на всю ширину (inner pad 12/11), лого и CTA display:none — как оригинал; header ≈87px
- ОТСТУПЫ КОНТЕЙНЕРА: 64→36 десктоп, 26 планшет (новый $container-pad-lg + шаг в mixin), мобилка 24 оставлена (допущение); кейс-страница: все офсеты 88px*k → 72px*k
- HERO НОВЫЙ ТЕКСТ: EN «First Your [Website Sells] / Then You [Pay]» (вариант A), UA «Спершу Ваш [Сайт Продає] / Потім — [Оплата]»; ПОЛНЫЙ перемер (canvas): EN группы 3.60/5.20/5.31em, UA 5.33/5.46/6.81em → десктоп --fs-hero: min(140px, 9vw−6.5px) (UA l1 10.96em ≤ 100vw−72), 2560: 208px; мобилка EN ДЕРЖИТ 64px с 390 (3 строки [First Your][Website Sells][Then You Pay]), UA 62.6@390 (4 строки, отдельный --hero-fs 18.3vw−8.8px + --hero-lines: 4); em-капы 5.87/6.23 остались валидны, lg l2 кап 70→76vw («Потім — Оплата» 75.3vw одной строкой → UA планшет 3 строки); СТАРЫЕ UA-капы в main.scss УДАЛЕНЫ (были под «Онлайн‑Продажів»); якоря потока теперь var(--hero-lines) × var(--hero-fs)
- СЕКЦИИ: SectionModel → слот 2 (после hero); SectionLayers закомментирована в index.vue (вернуть легко); FAQ: вопрос про владение продуктом удалён из обеих локалей (faq.items[5]); UA statement: «після\u00A0запуску» nbsp — хвост не отрывается одним словом
- КАЛЬКУЛЯТОР (Figma 1107:5719): панель «A rough estimate» ЗАКОММЕНТИРОВАНА в шаблоне (скрипт цел), на её месте НОВЫЙ процент заказчика (r3/new percent.svg 427×336 → PercentArt.vue, currentColor); тилт как был (по секции), нагрев #3C0A09 теперь по ховеру НА процент (pointerenter на mark-wrap); ЧИПСЫ: цикл подсветки в перемешанном порядке (shuffle-очередь, 1.6s, transition 700ms) текст #791313 / бордер #3C0A09, reduced-motion — статично; подводка «Move the sliders…» → «These are the factors we weigh:» / «Ось фактори, які ми зважуємо:» (НА РЕВЬЮ заказчика)
- ФОРМА: табы r999 (пилюли), «Preferred contact» → «Telegram or other contact» / «Telegram або інший контакт», у PSlider новый prop showEnds (форма: false — серые подписи диапазона убраны)
- Проверки: hero-матрица 320…1720 × EN/UA вся зелёная (rows/clip/gap/ovf), свитчер переключает локаль, чипсы циклятся, процент краснеет, форма (радиус/лейбл/ends) ✓, кейс 36/864 ✓, билд и консоль чистые; UA statement 4 строки (хвост «після запуску.»)
- ВОПРОС ОТКРЫТ: mobile pad 24 vs оригинальные ~10-16 (сказано в допущениях)

## R26 (2026-08-31) — hero мобилки 64px по моку + ДЕПЛОЙ НА VERCEL — ГОТОВО
- Деплой: git-репо github.com/akulida-wq/overdone.it-e-commerce_build (SSH, https-креды не настроены) → Vercel https://overdone-it-e-commerce-build.vercel.app/ (zero-config, Nitro сам видит Vercel); пуш в main = автодеплой
- Hero md: --hero-fs = min(64px, calc(18.8vw − 9px)) на .section-hero — 64px от 390 и выше, ниже — флюид с вычетом КОНСТАНТНЫХ паддингов (голый vw дрейфует — та же грабля, что 1025–1439); ВСЕ якоря секции (min-height 4×fs+12, cart top 132px+4×fs) выведены из переменной — UA-оверрайд перевешивает одну переменную и секция перепривязывается сама
- Капы строк EN переведены в em (5.87/6.23) — держат мок-переносы [Your Online][Sales System][With No][Upfront Fee] при любом --hero-fs; классы капов на КОРНЕ SectionTitle (span.section-hero__line--N), внутренние .section-title__line — другие узлы
- UA: 64 не может — «Онлайн‑Продажів» неразрывные 7.7em; --hero-fs: min(11.25vw, calc(12.9vw − 6.5px)) в main.scss @≤768
- ПОЙМАН СТАРЫЙ БАГ (жил и на проде): кап l1 71vw НИКОГДА не вмещал «Онлайн‑Продажів» (7.7em = 84.7vw при 11.25vw) — слово клипалось маской на 61px @390 → UA l1 кап = 100% (перенос [Система][Онлайн‑Продажів] и так естественный)
- ГРАБЛЯ КАСКАДА: равная специфичность scoped-компонента и main.scss решается ПОРЯДКОМ листов, который гуляет dev/build — локале-оверрайды в main.scss писать с удвоенным классом (.section-hero__line.section-hero__line--1)
- ГРАБЛЯ ЛЕНИСА-2: у скоупа страницы кейса lenis.scrollTo учитывает scroll-margin-top (см. R25) — здесь то же: якоря только маржами
- Замеры: EN 320–768 rows 4 clip 0 gap 24 (fs 51→64), UA 320–1024 rows 4 clip 0 (fs 35→113); прод перепроверен: en 64px/4/0, ua 43.81/4/0

## R25 (2026-08-31) — кейс: панель в потоке на мобилке, лого-стрелка, капы контейнеров — ГОТОВО
- Мобилка: панель больше НЕ fixed-оверлей — order:-1 + position:sticky В ПОТОКЕ над статьёй (grid column + align-items:stretch — ГРАБЛЯ: flex-start в колонке ужимает статью по контенту до 864 → текст за экраном), открытие РАЗДВИГАЕТ контент (замер: navH 42→273, artTop 202→433), отступы контейнера сохранены (24/24), карточка с бордером/скруглением, bg на __inner; sticky top 92 → is-raised top 8
- Иконка шапки = лого-стрелка заказчика (Frame 2147226756.svg, ромб-кольцо со стрелкой, inline currentColor): открыто — стрелка вверх, закрыто — rotate 180 (вниз); отдельный шеврон удалён
- Капы ширин: 1440 = МАКСИМУМ контейнеров — article max-width 864px и панель 304px БЕЗ --k, дальше растёт только зазор (замер: 1440 gap 144 → 1720 gap 424, ширины не меняются); скрин 641px фикс
- goTo: клик по пункту на мобилке сначала складывает бар (contentу над статьёй меняется высота — скролл до складывания промахивался на 231), потом скролл; ГРАБЛЯ ЛЕНИСА: lenis.scrollTo(el) САМ учитывает scroll-margin-top элемента — передача offset -148 ДВОИЛА поправку (элемент на 296 вместо 148) → offset убран, позиции задают только scroll-margin-top блоков (110 desktop / 148 md); остаточные −24px = translateY(24) v2-реивла блока в момент замера цели, безопасно
- Проверки: 390/480/640/768 art=nav=контейнер, ovf 0; 1440/1720 капы держатся; билд и консоль чистые

## R24 (2026-08-31) — кейс: сетка 2/3+1/3 по Figma, панель 304, планшет с панелью, мобильный фикс-бар — ГОТОВО
- Спека взята ЖИВЬЁМ из Figma через MCP (get_design_context, нода 1039:61236/1044:61364): статья 864 / панель 304 при контейнере 1312 (≈2/3:1/3), gap 48; H1 40 (-0.03em)/lead 24/H2 32/H3 24/текст 20 #7a7a7a; скрин 641 внутри колонки 864; панель: бордер rgba(161,163,165,.25) (= наш --color-border-table, совпал), r4, шапка 8×12/gap 10/иконка 20/18px/1.5 красная, пункты h36/px12/16px/1.5/r4, активный залив 0.07 на всю ширину
- Раскладка: article flex:1 max-width 864k / панель 304k, между 1025–1439 статья сжимается первой (проверено: без переполнений)
- Закрытый стейт целый: разделитель перенесён с .case-nav__collapse-in на .case-nav__list (border-top у collapse-in при grid-rows 0fr НЕ клипается — торчал 1px; у list внутри overflow:hidden клипается) — замер: inner−head = ровно 2px бордера
- Планшет ≤1024: панель НЕ прячется — 240px, шапка 16px, gap 32 (display:none снят)
- Мобилка ≤768: панель = fixed бар на всю ширину под хедером (top 84 = высота мобильного хедера 83), z-index 90 (хедер 100), bg --color-bg, только нижний бордер; свёрнут по умолчанию (matchMedia в onMounted), тот же шеврон-тогл; при скрытии хедера едет в top 0 — ГРАБЛЯ: мобильный хедер absolute и до y=120 просто уезжает — порог подъёма на мобилке 84 (= высота хедера), иначе щель-полоса при 84<y<120; scroll-margin блоков 128, padding-top страницы 168
- UA nav_title восстановлен «Навігація сторінкою» (в 304/240 влезает одной строкой, замерено 1440/1024/390 ovf 0)
- Проверки: 1440 art 864/nav 304/flush 64-64; 1024 art 624/nav 240; 390 бар top 84→0 (вниз)→84 (вверх, хедер вернулся), открытие 272px; билд и консоль чистые

## R23.1 (2026-08-31) — кейс: ссылка карточки + возврат к размерностям макета — ГОТОВО
- Карточка Paparazzi на лендинге (клик по карточке + кнопка Website) вела на paparazzi.shop → заменено на https://pprz-school.overdone.it/ (реальный адрес сайта)
- «Фото 1 vs фото 2»: заказчик показал, что типографика кейса должна быть КАК В МАКЕТЕ 1039:61236, а не «H1=56». Макет = токены лендинга на ступень ниже: H1 кейса = $fs-h3 (40), заголовки блоков = $fs-h4 (32), lead 24, текст 20 — замерено 40/32/24/20
- Плотный ритм по макету: H1→lead 16, lead→кнопка 24, кнопка→первый H2 56; gap статьи 56, gap блока 16, скрин margin-top 16
- Eyebrow «Case · E-commerce Partnership» убран (в макете его нет)
- Шапка панели: 16px + nowrap, одна строка при 188px; UA nav_title сокращён до «Навігація» (иначе «Навігація сторінкою» не влезает)
- md-оверрайды кеглей удалены — токены _scale.scss сами даунстепят (h3 32 / h4 26→24)
- ВАЖНО: mention «paparazzi.shop» остался в текстах результата (case_pprz result) — заказчик упоминал его как новый домен, на его ревью

## R23 (2026-08-30) — кейс: левый край, «едущая» складная навигация, шкала заголовков — ГОТОВО
- Сетка: статья ПО ЛЕВОМУ краю контейнера (64), навигация по правому (space-between) — на широких экранах зазор растёт (замер: 483 @1440 → 763 @1720)
- Навигация «едет» за хедером: свой scroll-листенер (порог 120, та же логика, что в PHeader) → .is-raised { top: 16 } c transition top 300ms — когда хедер прячется, панель поднимается на его место и обратно
- Навигация складывается: шапка «Page navigation» = кнопка (aria-expanded) с шевроном, список в grid-rows 0fr→1fr 350ms (замер: 305↔75)
- Заголовки в шкале лендинга: H1 = $fs-h2 (56), заголовки блоков = $fs-h3 (40); md — h2-m/h3-m
- Замеры: artLeft 64, navRight 1376, h1 56/h2 40, консоль чистая, overflow 0 (1440/1720)

## R22 (2026-08-30) — КЕЙС-СТРАНИЦА PAPARAZZI ПЕРЕСОБРАНА (Figma 1039:61236) — ГОТОВО
- pages/projects/paparazzi.vue переписана с v1 на макет «статьи»: слева колонка 641k (eyebrow → H1 40 с красным «Paparazzi» → лид 24 → PButton «Visit Website» на pprz-school.overdone.it → секции H2 32 + абзацы 20 muted + скрины в рамках r12 border-table), справа STICKY «Page navigation» 188k (Figma 1044:61315: шапка с красной звёздочкой bg white/08, нумерованные пункты 36px, актив bg white/07) со скроллспаем (IO rootMargin −20%/−70%) и кликом через lenis.scrollTo(offset −110); замер 1440: art 641 @left 288, nav 188, gap 36 — 1в1 с макетом
- КОНТЕНТ: 6 секций «завлекающего» текста EN+UA в case_pprz.sections (Огляд / Вітрина та каталог / Сторінка товару / Панель керування / Синхронізація з обліком (BAS, «понад 27 тис товарів» — факт со скрина адмінки) / Результат — партнёрская модель, paparazzi.shop); тексты написаны мной, заказчик просил «завлекающий текст что мы сделали» — на ревью заказчика
- АССЕТЫ: 5 свежих скринов заказчика → public/img/case-paparazzi/{home,catalog,product,admin-promos,admin-products}.webp (2000px, старые v1-скрины перезаписаны; TODO(USER) про адмін-скрины закрыт)
- НИЗ 1в1 с лендингом: <SectionContact /> + <PFooter /> (общие компоненты; PHeader-кнопка Contact скроллит к #contact прямо на кейсе)
- Адаптивы: ≤1024 навигация скрыта, статья на всю ширину; md — H1 36/H3-m, padding 128; замеры: overflow 0 на 1440/390ua, консоль чистая; eyebrow-грабля: у SectionEyebrow проп label (не text)

## R21 (2026-08-30) — ГОТОВ
- HERO десктоп: корзина теперь ЦЕНТРИРУЕТСЯ в зоне хедер→кнопки по ОБЕИМ осям и заполняет её по высоте (кап 90vw) — на высоких окнах висела маленькой в верхней половине. width min(90vw, (100svh−286)·1.804); top calc((100svh − 94px − min(49.9vw, 100svh−286px))/2); left 50% (было 46.8vw). При 1440×900 разрешается в исходную R4-геометрию (96..711, было 88..708). Замеры 1280×800/1440×900/1600×1000/1720×940: топ 96, низ 61 над кнопками, cxOff 0; высокие 1995×1804 и 1280×1300: по центру зоны, карт больше (1796/1152), overflow 0

## R20 (2026-08-30) — ГОТОВ
- S9 %-тилт: реакция на курсор ×1.5 — углы ±9/±6 → ±13.5/±9 (lerp .07 прежний)

## R19 (2026-08-30) — ГОТОВ
- HERO мобилки: композиция КОНТЕНТНАЯ (Figma-компакт), без svh-якорей — title под хедером (96), корзина под title (top calc(132px+45vw)), кнопки под корзиной (bottom margin-top calc(62vw+56px)); min-height 0; title min-height calc(45vw+12px) резервирует 4 строки (EN на части ширин падал в 3 — константы разъезжались). Замер: title-cart 24, cart-кнопки 31 на любой ширине ≤768 и любой высоте
- ПЕРЕНОСЫ ЗАГОЛОВКА по фото 2: [Your Online][Sales System]/[With No][Upfront Fee] и [Система][Онлайн-Продажів]/[Без Оплати][На Старті] — суженные строки (l1 66vw, l2 70vw; UA в main.scss l1 71vw / l2 64vw) + у акцентов на md снят nowrap (блокировал перенос внутри [Online Sales]). ДВЕ ГРАБЛИ: (1) хвостовой сепаратор после акцента при впритык-капе уезжает ПУСТОЙ строкой внутрь маски (бокс тихо растёт на 1em); (2) укр. слово ломалось ПО ДЕФИСУ на 2 строки, а word-probe по пробелам это НЕ видел (рект слова накрывает обе строки) — в ua.json неразрывный дефис U+2011 (визуально идентичен; на десктопе перенос даже улучшился)
- Замеры: 390/700/768 EN+UA — ровно 2+2 строки, отступы 24/31, overflow 0
- ДИАПАЗОН 1025–1439: hero-строка снова ломалась — флюид min(140px, 8.88vw) не учитывал КОНСТАНТНЫЕ паддинги контейнера (100vw−128): при уменьшении окна кегль падал медленнее контейнера → перенос. Формула теперь контейнерная: --fs-hero: min(140px, calc(9.7vw − 12.5px)) (замер 1100–1600: l1/l2 по одной строке, 1440 = 127.2, ≥1577 = 140)
- ПЛАНШЕТ (769–1024): тот же макетный перенос — капы l1 66vw / l2 70vw + accent-wrap продублированы в lg-блок; UA-капы в main.scss расширены до ≤1024. Замер 800/912/1024 EN+UA: [Your Online][Sales System][With No][Upfront Fee] / [Система][Онлайн-Продажів][Без Оплати][На Старті] ✓

## R18 (2026-08-30) — ГОТОВ
- S5 statement: шаг между строкой 1 и 2 отличался от 2→3 — flex gap 15px сидел ПОВЕРХ line-height 1 (межблочный питч 55 vs внутренний 40). gap убран, line-height 1.15 — единый питч (замер: EN [46,46], UA [41,42,41])

## R17 (2026-08-30) — ГОТОВ
- S5 UA (фото 1): длинная укр. первая строка ломала право-выровненную композицию — html[lang='ua'] .section-model__statement { font-size: 36px } при ≥1025 (замер: l1 в ОДНУ строку; ≤1024 свои ступени). ГРАБЛЯ: :global() в scoped-стиле скомпилировался в ГОЛЫЙ html[lang]{font-size:36px} (задел корень!) — правило живёт в main.scss
- S8 (фото 2): ховер-цвет кубиков 3C0A09 → #4C1919 (замер: transition → rgb(76,25,25))
- S9 %: 3D-тилт НАКОНЕЦ виден — perspective должен стоять на ПРЯМОМ родителе вращаемого слоя (был на дальнем предке __inner → рендер плоский, «не 3д»); perspective: 900px на __mark-wrap + углы ±9/±6 (замер: rotateX 5.7°)
- S9 % нагрев: ТОЛЬКО над самими ползунками — listeners на каждой .p-slider__zone (не на всей панели; замер: над заголовком панели холодный, над зоной — горячий); цвет → #3C0A09 вместо чистого акцента
- ГРАБЛЯ ПРОБ (повторно подтверждена): getComputedStyle читает ПРОМЕЖУТОЧНОЕ значение транзишена и легко читается НЕ ТОТ элемент (первая карточка vs :hover-карточка) — мерить через '.x:hover .y' селектор + сэмплы по времени
- Смоук 390ua/768en: overflow 0, консоль чистая, statement на адаптивах свои 28px

## R16 (2026-08-30) — ГОТОВ
- Табы формы (фото 1): на ≤480 ВЕРТИКАЛЬНО (flex-direction column) — рядом каждая ломалась в две строки
- ЛОГО у футера на телефоне (фото 2): +130% — НАЙДЕНА ГРАБЛЯ: глобальный reset `img { max-width: 100% }` молча капал ВСЕ >100vw ширины лого (поэтому прошлые «увеличь» не срабатывали!) → __bg { max-width: none }; на ≤768 width 170vw, left −15vw (обрезается справа ✓ замер bgW 663@390); планшет теперь честные 130vw (1170@900)
- Футер-аккордеон: пункты в открытой группе СЕРЫЕ (--color-text-secondary)
- UA hero (фото 3): Title-Case как в EN — «Система [Онлайн-Продажів]» / «[Без Оплати] На Старті» (locales/ua.json); кегль ПЕРЕмерен под ставшие шире строки: md 11.25vw, lg 11.05vw — 4 строки без OVF в обеих локалях на 390/768/900 (замер)
- S7 (фото 4): закрытые пункты — лишние 28px слева: md-правило `__num { width: 28px }` перебивало схлопнутый width:0 (специфичность+порядок) → правило переписано на `.is-active .scope-row__num` → замер 16/16 симметрично; анимация не тронута
- Смоук: 390ua/390en/768ua/900ua — overflow 0, консоль чистая

## R15 (2026-08-30) — ГОТОВ
- HERO мобилки («найди отличия» vs Figma-мок): корзина МЕНЬШЕ (width min(112vw, 56svh·1.804) — арт ≈ ширина экрана) и НИЖЕ (top clamp(300px,41svh,430px)) — между заголовком и кнопками с воздухом, 1в1 с моком (скрин сверен)
- S2 мобилка (фото 3): кнопки карточек на всю ширину — md &__buttons { width 100%; > * { flex 1; justify-content center } } (замер: 138+138 в 292 враппера = вся карточка; одиночная Discuss тоже full)
- S4 «опять обрезается» (фото 4) — КОРЕНЬ НАЙДЕН: v2-reveal на КАРТОЧКАХ внутри горизонтального скроллера — карточки за правым краем не пересекали вьюпорт, батч их не открывал, оставался pre-hide translateY(24px) → низ резался overflow-y:auto контейнера. Reveal перенесён на САМ __ribbon (один элемент), карточки без reveal-класса (в пине конфликтов нет — gsap y владеет карточками). Замер: скролл до конца — anyCut false, lastBottom==ribbonBottom ✓
- Compare (фото 5): __dots margin-top 32→24 ($spacing-6)
- FAQ (фото 6): faq-card__para 18→16 на ≤768 ($fs-body)
- Смоук 390en/768ua/1440en: overflow 0, консоль чистая, S4-карточки все видимы, пины живы

## R14 (2026-08-30) — ГОТОВ
- ВАЖНОЕ УТОЧНЕНИЕ ЗАКАЗЧИКА: «свг поярче + ховер 3C0A09» относилось к S8 «How the partnership begins» (кубики StepArt), НЕ к S4! → StepArt переведён на currentColor; .step-card__art { color: color-mix(surface-2 68%, muted) ≈ #3b (было #1d1d1d), transition 350ms }; .step-card:hover &__art → #3C0A09 (работает и в карусели, и в статик-сетке). Замер: base srgb .23, hover rgb(60,10,9) ✓
- S4 «The entire technical side on us»: ховер УБРАН совсем (был мой цветовой ховер из R10-ошибки); светлый базовый тон иконок оставлен. Замер: цвет на ховере не меняется ✓
- ЗАГОЛОВКИ, обрезание по высоте («опять — это пиздец»): у SectionTitle __line глубина под десцендеры была 0.16em, а у SF Pro хвосты p/g/y ≈ 0.21em → на некоторых кеглях резало. Теперь padding 0.12em/0.25em (+ отрицательные маргины — метрики не меняются). ГРАБЛЯ-ЗАМЕТКА: мой клип-сканер ИСКЛЮЧАЛ section-title (слепая зона) — потому «0 порезов» при живом дефекте; скрин «How the partnership begins» — хвосты целы ✓
- ЛОГО у футера: ещё ниже на ~140px — bottom max(-620px,-43vw), видно ~65% (замер visibleFrac 0.65)
- Смоук 390ua/768en/900ua: overflow 0, консоль чистая

## R13 (процент в калькуляторе — интерактив) — ГОТОВ (2026-08-30)
- ТИЛТ ПО ВСЕЙ СЕКЦИИ: mousemove теперь на rootEl секции (был только над svg — заказчик не видел эффекта); цели наклона считаются от центра markWrap с клэмпом ±1 (rY ±6 / rX ±4, lerp .07), mouseleave секции → 0. Замер: transform различается в двух точках секции ✓
- ПОКРАСНЕНИЕ ОТ ПОЛЗУНКОВ: percent.svg ИНЛАЙНЕН в components/ui/PercentArt.vue (3 path fill=currentColor, геометрия дословно из файла заказчика); .section-calc__mark { color #1e1e1f; transition color 600ms } + .is-hot → var(--color-accent). Триггеры: pctHot = sliderHover (pointerenter/leave на .calc-panel__sliders) ИЛИ sliderPulse (watch [margin,volume,aov,integrations] → таймер 700ms — ловит мышь/тач/клавиатуру). Замеры: hover → rgb(241,38,37), уход → rgb(30,30,31); стрелка на слайдере → пик 179,36,35 → остывание ✓
- img заменён на <div ref=markEl><PercentArt/></div> (флоат y±12 на диве); wrap теперь ЦЕЛИКОМ pointer-events none (тилт слушает секцию — ничего не перекрывает); public/img/v2/percent.svg остаётся как исходник

## R12 (восьмая пачка + самоаудит, 2026-08-30) — ГОТОВ
- Classic CTA: «Learn the details» нелогично для кнопки на форму → v2.classic_cta = EN «Start the classic way» / UA «Почати у класичному форматі» (ссылка на overdone.it/#contact без изменений)
- S9 % (зелёная рамка заказчика): НИЗ левой половины по центру — wrap { left 0; bottom $spacing-6; width 46%; flex center } (был inset-block 0 + center → на высоких окнах налезал на чипсы); анимация живёт с R10 (float y±12 4.5s sine + тилт над областью svg) — подтверждена скринами/замерами
- ЛОГО у футера на десктопе: ОПУЩЕНО до «видно 4/5» — bottom max(-480px,-33vw) + `.app { overflow: clip }` в main.scss (абсолютный низ кольца раньше РАСТЯГИВАЛ документ — клип на корневом диве режет его на границе страницы; fixed-пины ScrollTrigger клипом не задеваются — containing block viewport). Замер: visibleFrac 0.81 ✓
- HERO мобилки (Figma 1028:61207): заголовок СРАЗУ под хедером (margin-block 0 auto, padding-top 96), корзина по середине (top clamp(230px,32svh,380px), width min(150vw,64svh·1.804)), кнопки внизу — 1в1 с макетом (скрин снят)
- S4 иконки: ЗАМЕТНО светлее — color-mix(muted 45%, secondary) ≈ srgb .575 (было .53); hover #3C0A09 живёт (замер rgb(60,10,9)); других ховер-эффектов на карточках НЕТ (только цвет свг — как просили)
- САМОАУДИТ R6–R12 (18 проверок, все PASS): S2 стретч 632→900+бордеры/фулблид+фейды/клик paparazzi.shop; S3 баланс (43/70 у кромки анпина — лаг скраба, в пине 43/48); S4 старт 64/128/64 + высоты [231] + правый край 1377; S5 клик пик R=60 (#3C0A09); S7 плавный индекс op→1; S8 снап = ровно 1 карточка; earn 1,1,1,1; табы/каналы бордеры; планшет UA (4 строки hero, S2-заголовок отдельно, «& more» 24px, футер-аккордеон, % скрыт, overflow 0); мобилка UA (4 строки, хедер 83px одна строка, & more скрыт, titleTop 96, S8 5/5, обрезаний текста 0); консоль чиста на всех трёх
- КОНТЕКСТ-ДОК: этот файл (SESSION_CONTEXT.md) и ведётся как полная память проекта — обновляется каждый круг; заказчику сказано

## R11 (седьмая пачка прямых правок, r6-s8…s17) — ГОТОВ (2026-08-30)
- S8 (r6-s8): статик-карточки ПРОПАДАЛИ на мобилке — v2-reveal (общий батч мог отстрелять оффскрин при загрузке) заменён на СВОЙ IntersectionObserver (js-step-static, паттерн earn: set autoAlpha 0/y24 → to 1/0 stagger .12 при входе; armStaticReveal на mount + watch(motionMode)); замер: 5/5 visible после скролла ✓. Глобальный скан обрезаний текста по высоте на 390ua/768en/768ua — НОЛЬ порезанных элементов (маски заголовков несут компенсацию десцендеров с R3)
- S10 (r6-s9): арты на мобилке 130→100px (:deep(svg) в md)
- ЛОГО у формы (r6-11/s16): на ≤1024 кольцо ОГРОМНОЕ — width 130vw, right −8vw (левый край и низ обрезаются — так задумано), bottom −34vw
- HERO ≤768 (r6-s12): кегль 11.6vw ПРОПОРЦИОНАЛЬНО (52px-кап ломал переносы на портретных планшетах); 768: EN 3 строки/UA 4, 390: 4/4 ✓
- S3 «& more» (r6-s13): стиль = имя подписи (24k, muted, ls −0.03) — не выбивается; на ≤640 скрыт совсем
- СКРОЛЛБАР (r6-s15/16): «справа отступ больше» = классические скроллбары (100vw > clientWidth → full-bleed сидел на полскроллбара левее) — app.vue пишет --sbw (innerWidth−clientWidth) на :root (mount+resize), S2-рамка и пин-паддинги S4 считают через calc(100vw − var(--sbw)); на overlay-скроллбарах sbw=0 → поведение прежнее
- ХЕДЕР планшета (r6-s15): лого и кнопка flex 1 1 0 (лого влево, кнопка вправо) — плашка по центру (offset −13px на UA из-за min-content кнопки, EN ~0); на md лого flex 0 0 auto (возврат)
- ФУТЕР (r6-s16): аккордеон расширен на ПЛАНШЕТЫ — mq и CSS-блок 768→1024 («или в 2 ряда или аккордеон» — выбран аккордеон); Back to top больше не режется, overflow 0
- Ползунок (r6-s17): УЖЕ и НИЖЕ по макету — σ 9(+5 hover), подъём 5(+3h+2d), красная зона радиус 36(+12) (было 16/8/64+16)
- Classic development CTA: «How it works» → v2.classic_cta (EN «Learn the details» / UA «Дізнатися деталі»), href https://overdone.it/#contact target _blank (форма ГЛАВНОЙ overdone.it; ЯКОРЬ #contact — ПРОВЕРИТЬ у заказчика, добавить в HANDOFF-TODO при переносе)
- QA: S8 мобилка 5/5, футер-аккордеон на 900 (grid-collapse ✓), hero-строки 4/3-4, «& more» flex@768/none@390, порезов текста 0, desktop-регрессия (sbw 0px, frame 1440, пины 1111, classicHref ✓), консоль чистая везде

## R10 (шестая пачка прямых правок, фото r6-s0…s10 + percent.svg) — ГОТОВ (2026-08-30)
- ЛОГО у формы (r6-s0 vs s1): композиция 1в1 с макетом — кольцо БОЛЬШОЕ (min(64vw,1150px)), left 40vw, bottom max(-290px,-21vw): верх-лево прячется ЗА панелью формы, петля выходит под формой и уходит за футер до низа страницы, полоска в правом жёлобе (правый свес режет body-клип — так в макете); низ не вылезает за конец документа (замер bottomInDoc≤docH на всех ширинах)
- ПОЛЗУНКИ: диапазоны отображения выровнены с подписями краёв — contact EN [5k,100k] (был 150k при подписи «$100k+» — БАГ с фото), UA [50тыс,10млн] (был 3млн при «10+ млн»); calc volume EN [5k,400k] (был 500k), UA [50тыс,10млн] (был 12млн); AOV EN [50,2000] (был 2500 при «$2k+»), UA [500,20тыс] (был 25тыс). Замер: макс контакт-слайдера теперь «≈ $100k» ✓
- S4 (r6-s2): IconService переведён на currentColor; карточка задаёт цвет иконок color-mix(muted 72%, secondary) — «чуть светлее», hover карточки → #3C0A09 350ms (замер: base srgb .53, hover rgb(60,10,9)); имя карточки принудительно --color-text (не наследует тонировку)
- S9 % (r6-s3/3.2): встроен ПРИСЛАННЫЙ public/img/v2/percent.svg (контурный, #1E1E1F) — по центру левой половины ПО ВЕРТИКАЛИ (wrap absolute inset-block 0 + flex center), размер min(30vw,480px) + max-height 56svh (не шире панели: 432 vs 640 ✓); анимация «еле-еле»: float y±12 4.5s sine yoyo на img + 3D-тилт ±6/±4 lerp .07 на tilt-слое ТОЛЬКО при курсоре над областью процента (listeners на wrap, wrap pointer-events none / tilt auto — заголовок и чипсы не перекрыты)
- ХЕДЕР мобилки (r6-s4): ОДНА строка — лого svg 34px, плашка с 4 линками В ОДИН РЯД (12px, nowrap, паддинги 4/3) + тикер 18px/10px, круглая кнопка 40px; высота хедера 83px, UA влезает
- HERO (r6-s5): кегль ПОДОБРАН ЗАМЕРОМ переносов (probe по line-count в обеих локалях): планшет 11.3vw, мобилка min(52px,11.8vw) — ровно 4 сбалансированные строки EN и UA, без переполнений
- S4 лента (r6-s6): в статик-режиме маска-фейд по краям (linear-gradient 40px, -webkit- ПЕРВЫМ) — прокрученные карточки растворяются у краёв, а не режутся (красные линии на фото заказчика — аннотации, реального вертикального клипа нет, проверено)
- ШРИФТОВЫЕ ЯРУСЫ (r6-s7/10): аудит по всем секциям — compare __text 18→16 при ≤768 (+IconMs 24→20), earn-cell __name 24k→body-lg, calc __lead 24k→body-lg-m, step-card __num 24k→body-lg; правило ярусов: 24→18(-m), 20(var body-lg)→18 авто, 18→16, 16(var body) остаётся
- QA: 1440/1340/900/768ua/390ua — консоль чистая, overflow 0 (1340: 2px невидимого хвоста под body-клипом от намеренного правого свеса лого), S4 heights [231] uniform на 1340 (кейс фото 1 невозможен), логотип в границах документа везде; мобилка: хедер 83px одна строка, hero 4 строки, кнопки на всю ширину

## R9 (пятая пачка прямых правок, фото 1–10 + PNG лого + SVG-примеры) — ГОТОВ (2026-08-30)
- АССЕТ: заказчик прислал ПОЛНЫЙ рендер кольца-лого (2000×2000 webp с альфой, был в чате) → public/img/v2/footer-logo.webp (trim+resize 1400, sharp). Заменяет обрезанный фигмовский form-3d.webp за формой/футером
- S12+футер (фото 2): лого НЕ обрезается нигде — width min(46vw, 820px) (капится вьюпортом), right 3vw, bottom max(-200px,-18vw) — свес за футер КОРОЧЕ футера, за конец страницы не уезжает (проверено bottomInDoc<docH на 1440/1024/768/640/390)
- S12: табы формы — ОДИН общий строук вокруг обеих (border на .contact-panel__tabs, r12), сами табы border transparent margin −1px, активная рисует красный поверх; углы Book a Call/Telegram больше не режутся — бордер на КОНТЕЙНЕРЕ __channels (r12+overflow hidden), ряды только border-top divider
- S10 (фото 3): IconLine ПЕРЕРИСОВАН в стиле SVG icon example 1/2 — изокуб-хелперы hex()/ribs()/cube() (C=0.866), вложенные opacity-.5 слои, скругления, один красный акцент; смыслы прежние (percent=куб+2 миникуба+красный слэш, growth=лестница кубов+стрелка, renew=два куба+дуги цикла, balance=балка+кубы на подвесах); viewBox 0 0 96 96
- S10 (фото 9): появление карточек слева-направо ПЕРЕВЕДЕНО на IntersectionObserver (rootMargin -18% низ) — прежний ScrollTrigger с once:true считал start ДО вставки pin-спейсеров и мог отстрелять оффскрин при загрузке → юзер не видел анимации. Замер: 0,0,0,0 → 0.6,0.2,0,0 → … → 1,1,1,1 (stagger .15, dur .7) ✓
- S4 (фото 1): у имени карточки на lg min-height 2.4em (2 строки×1.2) — однострочные контейнеры той же высоты, что и двухстрочные
- S3 (фото 4): подписи на ≤1024 = ТАБЛИЦА — grid gap 1px, background=border-table (1px-линии), ячейки красят --color-bg, border вокруг; 8-я ячейка «& more» (locale v2.layers_more: en «& more» / ua «та інше»), скрыта >1024 (специфичность: базовый display:none 1-классный, lg-flex позже в файле)
- HERO (фото 5/7): планшет ≤1024 — заголовок 12.5vw ПО ЦЕНТРУ (обе строки), корзина min(130vw, 66svh·1.804); мобилка ≤768 — 15vw по центру (переносится в 4-6 полноширинных строк), корзина min(150vw, 70svh·1.804), секция max(560px, 92svh), кнопки СТОЛБИКОМ на всю ширину (actions column stretch, >* width 100% justify center), note по центру
- ХЕДЕР мобилки (фото 7): ОДИН уровень — лого | плашка (линки 2×2: li flex 0 0 50%, тикер 22px) | круглая кнопка звонка; двухуровневый R7-вариант удалён; высота ~116px, hero padding-top 128
- S2 (фото 6): на ≤1024 заголовок НЕ ячейка — отдельный SectionTitle над таблицей (дубль в шаблоне: static скрыт ≥1025, ячейка скрыта ≤1024 — в a11y-дереве всегда ровно один); у первой карточной ячейки убран верхний divider
- ФУТЕР (фото 8): на ≤768 АККОРДЕОН — 4 группы строками с табл: первая открыта, тап по другой закрывает прежнюю (паттерн scope), плюс→минус (::before/::after rotate), высота grid-rows 0fr→1fr 350ms; десктоп не тронут (__items display:contents)
- S9: % -знак за левой колонкой (фото-референс) — svg 2 кольца+слэш stroke #1D1E20 w20, АНИМАЦИЯ 1в1 корзина: gsap y±10 sine 3s yoyo на svg + 3D тилт rotateY±6/rotateX±4 lerp .07 на обёртке (perspective на __inner). ЯКОРЬ: __inner position relative (левая колонка всего ~316px — bottom% от неё сажал знак на заголовок!); скрыт ≤1024; left 4% bottom 6%
- Ползунки (фото 10): экспонента 12 + σ 16/+8 — почти прямоугольный блок с вертикальными краями («резко» как на макете)
- QA: планшет 900×1300 (все чек-поинты числами), мобилка 390 EN/UA, 640ua/768en/1024ua — overflow 0, лого влезает, консоль чистая; десктоп 1440 регрессия чистая (заголовок в ячейке, static скрыт, %-знак на месте, пины 1111); аккордеон футера 1000→0100; earn-ramp покадрово ✓

## R8 (четвёртая пачка прямых правок, фото 22–34) — ГОТОВ (2026-08-30)
- HERO (фото 22): min-height max(640px, 100svh) — кнопки/подпись всегда в первом экране; шрифт --fs-hero: min(140px, 8.88vw) — 140 как на живом overdone.it, но флюидный кап ниже ~1577px (при жёстких 140 на 1440 строка «Your Online Sales System» ФИЗИЧЕСКИ не влезает в контейнер 1312 — 128 её точный размер; сказать заказчику); корзина капится высотой окна: width min(77.66vw, (100svh−170px)·1.804), центр 46.8vw margin-центровкой (transform у тилта)
- ХЕДЕР: блюр убран совсем — только linear-gradient прозрачный→bg снизу вверх
- S2 (фото 23): рамка FULL-BLEED на десктопе (width 100vw, margin-left 50%−50vw) — строуки от края до края экрана; по бокам ::before/::after градиенты в цвет фона (clamp(48px,9vw,180px)) вместо жёсткого среза; трек padding-inline max(64px,(100vw−container)/2) — старт по контейнеру, финал по правому краю контейнера; body overflow-x: clip (страховка 100vw на классических скроллбарах; clip — не hidden, пины не задеты)
- S3 (фото 24): центрирование открытого стека ЖИВЫМ ЗАМЕРОМ (placeStage: margin = (innerHeight − headBottom − визуальная высота стека)/2, clamp 40..200) на init + onRefreshInit — CSS-clamp с константой 815 плыл на широких экранах, где заголовок в одну строку (headBottom 130, а не 192); замер 900: 43/48
- S4 (фото 25/26): карточки ОДНОЙ высоты — трек align-items: stretch + на десктопе width:auto/min-width:416k + name nowrap (карточка растёт под своё имя в одну строку; lg/md — фикс-ширины, перенос в 2 строки, stretch равняет); последняя карточка заканчивается ПО ПРАВОМУ КРАЮ КОНТЕЙНЕРА — track padding-inline вместо только left (замер: heights=[231] uniq, lastRight 1376 == container right)
- S5 (фото 27): клик красит фигуру в #3C0A09 (не 100% акцент); ГЛОБАЛЬНО ::selection = rgba(241,38,37,.35) вместо сплошного акцента
- S7 (фото 28): красный индекс раскрывается ПЛАВНО — width 0→32 + margin-right −gap→0 + opacity 0→1 400ms тем же cubic-bezier (вместо display:none toggle, который дёргал строку); в lg-блоке margin-right −$spacing-5 под меньший gap. ГРАБЛЯ ПРОБ: getComputedStyle СРАЗУ после добавления класса возвращает СТАРТОВОЕ значение транзишена (0) — читать после завершения (это не «стили не применились»!)
- S8 (фото 29–31, daoism): визуал карточки перенесён НА __tilt-слой (padding/bg/border/radius/overflow) — тилт крутит саму карточку, gsap владеет внешним элементом (y/x/scale); статик-карточки --static несут поверхность сами (у них нет tilt); траектория «по кругу»: уход y−120 x−64 rot−6 blur12, приход fromTo y+120 x−64 rot+6 → 0 (левая дуга колеса, как на фигма-скринах); SNAP к ближайшей осевшей карточке (snapTo из tl.duration(), точки k−1+0.98, duration .15-.45 delay .1) — юзер не застревает между стейтами (замер: после паузы ровно одна карточка op=1); тексты справа: margin-left 48k
- S9 (фото 32): профиль крещендо «обрывистый» — exp(−(d/σ)^6) МЕСА с плоской вершиной и крутыми краями вместо гаусса; разделители панели калькулятора opacity .5
- S12+футер (фото 33 vs 34): inner justify-content: space-between + left max-width 640k с ≥1025 — форма ПРИШВАРТОВАНА к правому краю контейнера, зазор растёт с окном (1440: 32, 1720: 312); фигура form-3d поднята к верху блока (top 2%, left 58%, width 1560k) — от верха формы вниз за футер как на макете; табы чуть выше (padding-block 16)
- QA: 1440/1280ua/1024/768ua/390ua — overflow 0, консоль чистая, пины 1111 на десктопе / статика на адаптивах; ховер S7 замерен покадрово (0→0.08→0.42→0.83→1.0); S8 mid-стейт x/y/rot [−64/−120/−6] ✓

## R7 (адаптивы планшет/телефон, полный список фото 15–21) — ГОТОВ (2026-08-29)
- ШКАЛА (_scale.scss): добавлены НИСХОДЯЩИЕ ступени токенов — ≤1024: fs-hero clamp(56px,8vw,88px) (флюид, EN/UA строки влезают), h2 48, h3 32, h4 26, rhythm-half 64, btn 46/18; ≤768: h4 24, body-lg 18, rhythm 56, btn 44/17/22/10. Фиксированные -m токены компонентов не тронуты — ступени закрывают lg-диапазон 769–1024, где раньше был «сжатый десктоп»
- HERO (фото 15): на ≤1024 hero = 100svh, корзина сайзится ПО ОБЕИМ ОСЯМ — width min(112vw, 58svh·1.804) (аспект 1288/714), центр margin-центровкой left:50% + margin-left max(−56vw, 58svh·−0.902) (transform занят тилтом; auto-маргины прибивают >100vw бокс влево — over-constrained), top clamp(120px,22svh,300px); md: min(130vw, 62svh·1.804), top clamp(168px,24svh,300px), min-height max(640px,100svh), padding-top 168 (под двухуровневый хедер); sizes/preload imagesizes обновлены на 130vw/112vw/77.66vw
- ХЕДЕР: бургер и fullscreen-меню УДАЛЕНЫ СОВСЕМ. Планшет 769–1024 = полный десктопный, но плашка НЕ absolute, а в потоке (flex:1 1 auto, max-width 460, margin-inline auto) — лого/плашка/Book a call не пересекаются ни на какой ширине. Телефон ≤768 = двухуровневый: лого + КРУГЛАЯ красная кнопка звонка (48px, svg-трубка, aria-label) сверху; плашка (линки+тикер 26px) второй строкой на всю ширину контейнера (flex: 0 0 100% — full-basis форсит wrap, БЕЗ него max-width 460 с lg не давал переноса). UA-линки в 342px влезают (scroll==client 342)
- S2 (фото 16): на ≤1024 у рамки-«таблицы» появились БОКОВЫЕ строуки (border-inline)
- S3 (фото 17): подписи на ≤1024 = grid 2 колонки ПО ЛЕВОМУ краю (была центрированная колонка max-width 560); ≤640 — одна колонка (портретные планшеты 768–834 сохраняют две)
- S4 (фото 18): карточки уже — lg 348px, md 264px (сосед всегда срезан краем = виден горизонтальный скролл); эквалайзер-полоса теперь ЖИВЁТ и на нативном скролле ленты — listener scroll на __ribbon → progress = scrollLeft/max (в пин-режиме лента clip, scrollLeft всегда 0 — с таймлайном не дерётся). Проверено: первый тик rgba(189…)→белый при доскролле
- S5 (фото 19): на ≤1024 два ступенчатых спана схлопываются в ОДИН левый абзац (display block, line inline, lh 1.15); фоновая фигура 640→480 (lg) → 320 (md)
- S7 (фото 20): ≤1024 (или любой тач) = АККОРДЕОН типа FAQ — ПЕРВЫЙ пункт открыт, открытие другого закрывает предыдущий; плавная высота через grid-template-rows 0fr→1fr (desc обёрнут в __desc-in с min-height 0 + overflow hidden), 400ms тот же cubic-bezier; role=button/tabindex/aria-expanded только в аккордеон-режиме; гейт mqWide (min-width:1025) + hover/pointer, mq-listener на смену. Desktop hover-модель НЕ тронута (desc transparent + elementFromPoint)
- S8 (фото 21): статик-режим ПЕРЕПИСАН — сетка самодостаточных карточек (число/тег, изокуб 176 по центру, заголовок, desc ВНУТРИ карточки): 2 в ряд ≥640, 1 в ряд ниже; появление v2-reveal. Старый md-лейаут (арт в углу, desc снаружи, пустые карточки 280px) удалён. Десктопный reduce-фолбэк теперь тоже эта сетка
- QA: 1440 регрессия ЧИСТАЯ (плашка absolute 507, круглой кнопки нет, корзина 115/1118, statement flex, mark 640, scope desc transparent, hero 128px, все 4 пина живы); матрица 1280/1024ua/768ua/640/480/390ua — overflow 0 везде, консоль чистая; «клипы» скана = маск-контейнеры заголовков (глифы целы, проверено зумом). ГРАБЛЯ: scrollIntoView/scrollTo при живом Lenis откатывается — скроллить только колесом (wheel) или мерить от pin-spacer

## R6 (третья пачка прямых правок заказчика) — ГОТОВ (2026-08-29)
- S2 МЕХАНИКА РАСТЯЖЕНИЯ (V2-стретч возвращён): hairline-спаны УДАЛЕНЫ (лишний строук сверху ушёл); рамка НЕСЁТ border-top/bottom сама и в пине тянется fromTo(height: natural → 100svh, 0..0.3 power1.out), ячейки flex-stretch — разделители растут вместе с ней (замер: вход 587px с бордерами 1/1 → 893 в середине → 900 = вьюпорт); трек x стартует с 0.25 (перекрытие фаз); is-motion получил margin-bottom: var(--rhythm-half) → после анпина нижний строук виден и зазор до S3 = 80+80 как у всех (ScrollTrigger переносит margin в спейсер — computed 0, это норм); полноэкранные ::before-разделители R4 удалены (теперь сама ячейка на всю высоту)
- S2 КЛИКИ: обе карточки целиком role=link tabindex=0 (@click+@keydown.enter), Paparazzi → window.open(сайт), next → scrollTo('#contact') (проверено: клик в карточку открывает paparazzi.shop / приезжает к форме за ~1с); обёртки __buttons с @click.stop — кнопки не дублируют
- S3: открытый стейт опущен — margin-top сцены в is-motion = clamp($spacing-9, calc((100svh − 815px·k)/2), $spacing-15) — САМОЦЕНТРИРОВАНИЕ зоны под головой на любой высоте окна (замер: 900 → 48/37, 1050 → 112/123 сверху/снизу); фикс-токен балансировал одну высоту и ломал другие (815 ≈ низ головы + визуальная высота стека при k=1)
- S4: первая карточка ВРОВЕНЬ с контейнером — у трека в пине padding-left = calc((100vw − min(100vw − 2·pad, container-max))/2) (замер left 64 = заголовок/полоса; работает на всех ширинах: 1440→64, 1920→100, 2560→135); в начале таймлайна DEAD-ZONE 0.06 (оба твина с 0.06) — выровненный старт реально виден при захвате пина (по паттерну S3-hold); обрезание спущенных карточек ушло: у __ribbon overflow: visible + overflow-x: clip (низ 789 рисуется за лентой 743 ✓); ПОСЛЕДНЯЯ карточка выравнивается: дроп капится остатком пути трека remain=clamp((tx−endX)/600) smoothstep, y=min(e,remainE)·64 (замер в конце: y всех = 0, lastRight 1440 ровно); отступы: голова→полоса 128px (md: $spacing-11), полоса→карточки $spacing-11=64
- S5: клик-анимация длиннее ~1.3×: rotation 720° за 1.45s power2.inOut; цвет → акцент 0.3s power2.inOut, обратно 0.85s с 0.6 (замер сэмплами: R29→241 (акцент) → плавный спад 232/190/97/43→29, два оборота с точной посадкой в 720°)
- Проверено: пины живы (спейсеры 2430/3027/3150 = 170/220/250%), 390/768/UA/1440 — 0 переполнений, консоль чистая во всех прогонах, build чистый. ГРАБЛЯ ПРОБ: window.scrollTo(0,0) при живом Lenis ОТКАТЫВАЕТСЯ (внутренний таргет Lenis переезжает обратно) — базовые координаты мерить от pin-spacer'а свежей загрузкой, не после scrollTo; у секций БЕЗ пина parentElement не спейсер (хелпер спейсера давал мусор для S5)

## R5 (вторая пачка прямых правок заказчика) — ГОТОВ (2026-08-29)
- S5: фигуры S5+прелоадера перекрашены в #1D1E20 (loader-figure/logo-mark-big fill правкой sed); фигура S5 = CSS-MASK div (mask: url(svg), background #1D1E20) — перекрашиваемая; медленный спин на обёртке __spin, КЛИК по секции: gsap rotation += 720 power2.inOut 1.1s (разгон-замедление) + цвет вспыхивает акцентом 0.15s и возвращается 0.55s; разделитель заливается КРАСНЫМ (__divider-fill width = progress) синхронно со словами; скраб start 'top 85%' end 'center 50%' — при центре секции всё закрашено (замер 29/29 words, fill 100%); вертикальный баланс секции выровнялся сам после ритма 80 (замер 260/260)
- Ползунки (нода 983:55440): «хвост слева» УБРАН — красная зона СИММЕТРИЧНАЯ вокруг значения (радиус 64 юнита), при ховере/драге плавно ширится до 80 + тики выше (hoverAmt gsap 0.25s; гаусс swell 18→28 юнитов, высоты 8+8..20); значения с «≈» (prop approx на денежных/процентном); gap контейнера 16→8; замеры: red zone 80..111 тиков симметрично, hover 40 red / hMax 20
- S6: текст рядов 16→18px, IconMs 24; ховер карточки = scale 1.0125 (≈+8px) + подсветка bg/border 350ms ease-in-out; чип-марка слева от заголовка крутится −90° (красная: −45→−135) 450ms ease-in-out и обратно. ГРАБЛЯ: inline transform от общего sectionReveal (end-state без clearProps) душил hover-scale → карточки сняты с v2-reveal, собственный reveal с onComplete clearProps:'all' (pre-hide инлайновым gsap.set, БЕЗ CSS-прехайда — иначе clearProps вернёт скрытие)
- S7: контент пунктов по центру гор. оси (align-self baseline снят; замер центров num/name/desc/plus = 377/377/377/377); транзишены 400ms cubic-bezier(0.45,0,0.55,1) симметричные
- S8: StepArt ПЕРЕПИСАН на генерируемые изо-кубы в стиле SVG icon example 1/2 (helper cube(cx,cy,r) → hex+ribs; вложенные контуры, слои opacity .5; сцены: куб+лупа / куб+размерные выноски / два связанных куба / взлёт с платформы / лестница кубов+стрелка); арт 120% (263k) и поднят на 8px; карусель «ПО КРУГУ»: стопка убрана — активная уходит вверх (y−90 blur10 power2.in), следующая ПОДНИМАЕТСЯ СНИЗУ (fromTo y+90 blur10 → 0, power2.out, offset s+0.18); замер полусегмента: [−90/0, −1/0.99, +90/0] ✓
- Калькулятор: цифры результата при смене — подскок yPercent 30→0 + autoAlpha 0.25→1 0.4s power3.out (rangeEl ref)
- S10: появление ТОЛЬКО прозрачностью (0→1, без y), start 'top 92%', duration 0.6 stagger 0.12 — при центре секции всё видно (замер [1,1,1,1]); IconLine: viewBox ужат до 12..84 (рисунок заполняет контейнер), добавлены слои-детали op .5 (вторая окружность, вторая линия тренда, внутренние дуги, малые дуги весов)
- Ритм: --rhythm-half 64→80 (2560: 104) — «не так близко, но не сильно воздушно»
- S12+футер = ЦЕЛЬНЫЙ блок: contact padding-bottom $spacing-8, overflow-x: clip (НЕ hidden — CLS-правило пинов не задет, тут нет пина, но клип по X сохранён), фигура bottom −35% УХОДИТ ЗА ФУТЕР (замер: bg ниже секции на 278px, до футера достаёт, зазор секций 0); футер position relative padding-block $spacing-8; left-колонка @≥1441 max-width 640k (кнопки Book a Call/Telegram не тянутся); подпись→полоска ужаты (input padding 12/8, label top 12); чипсы ОБА таба по дефолту пустые
- Проверено: клип текста нигде (скан scrollHeight vs clientHeight — только visually-hidden намеренно); UA раунд-трип CLS 0.045, 0 переполнений; 390/768 чисто; консоль чистая; build 0 warnings

## R4 (прямые правки заказчика по скринам) — ГОТОВ (2026-08-29)
- HERO: корзина пропорционально уменьшена — вписана ПО ВЫСОТЕ между хедером (88) и кнопками (708): top 6.11vw, width 77.66vw (аспект 1288×714), left 7.97vw (центр сохранён); замер 88..709 против 88/708 ✓; sizes/preload imagesizes обновлены на 77.66vw
- ХЕДЕР (is-fixed): вместо плоской полупрозрачной заливки — linear-gradient(to top, transparent 0% → var(--color-bg) 100%) + blur 32, border-bottom убран
- НАХОДКА-БОМБА: `-webkit-backdrop-filter` в Chrome НЕ поддерживается (CSS.supports false), а cssnano при дедупе оставлял ТОЛЬКО -webkit-версию (стандартная объявлялась первой) → ВСЕ стеклянные блюры проекта не работали в Chrome с V2. Фикс: везде -webkit- ПЕРВЫМ, стандартное свойство ПОСЛЕДНИМ (мixins glass, хедер, нав-плашка, контакт-панель/каналы, compare) — computed теперь blur 32/28/64 ✓. ПРАВИЛО: prefixed до стандартного, иначе минификатор съедает рабочее свойство
- S2: разделители ячеек при пине — НА ВСЮ ВЫСОТУ ЭКРАНА (::before 100svh centered; рамка в моушене overflow-x: clip + overflow-y visible — clip не создаёт scroll-контейнер и не ломает CLS); отступ имя↔описание в карточке 20→12; ховер «Your product could be next»: имя тоже краснеет (правило расширено на обе карточки) + фон-картинка зумится 1.04 (глоу+точки перенесены на один слой __dots с transition как у img)
- S3: «карточка ужата по высоте» ПОДТВЕРЖДЕНА ЧИСЛОМ — при 60px в пин scaleY уже блендился в изометрию (644×347, ratio 1.856) → добавлен dead-zone hold 0.35 юнита в начале таймлайна (фазы сдвинуты: A 0.35, B/подписи 0.95+) → 639×359.5, ratio 1.778 (16:9) даже на +60px ✓; отступ голова↔сцена в пине 48→64; отступ имя↔описание подписей 20→12
- S4: отступ голова↔полоса ровно 96px; полоса переделана в ЭКВАЛАЙЗЕР — svg 180 тиков (viewBox 1440×24, client-only + SSR-заглушка), каждый тик растёт/уменьшается от прогресса (сигмоида на кромке ±1% + гаусс-бамп +6px), цвет серый→белый; вместо статичного каскада [0,0,32,64] — ДИНАМИЧЕСКИЙ заезд: карточки входят справа НИЖЕ на 64px и выравниваются в один уровень к 42% вьюпорта (smoothstep от x трека, БЕЗ DOM-чтений в кадре — offsetLeft кэш + gsap.getProperty x; замер y 0/0/21/64 слева направо ✓); иконки: контейнер 104 (120%), viewBox ужат до «5 4 79 79» (от края до края по ширине), CRM и Analytics перерисованы чисто
- Проверено: все замеры числами + скрины; ховер next (имя красное, зум 1.04, кольцо крутится); UA полный проход туда-обратно CLS 0.045 (<0.1), 0 переполнений, консоль чистая; 390 чист; build 0 warnings

## R3-P6 (полный QA по устройствам) — ГОТОВ (2026-08-29)
- Матрица 390/768/1024/1280/1440/1920/2560 × en/ua (14 комбинаций): reduce full-page скан — 0 переполнений, 0 малых тач-таргетов (<44), все 12 секций полны; моушен-проход колесом туда-обратно на ≥1024 — 0 ошибок консоли везде; скрины в скретчпаде qa/
- ГЛАВНАЯ НАХОДКА: CLS полного прохода был 1.93 (в V2 0.002!) — ДВА сдвига по 1.0 ровно на входе/выходе пина S4. Бисекция стилевыми инъекциями: виновник `overflow: hidden` НА САМОЙ ПИНЯЩЕЙСЯ СЕКЦИИ (Chrome репортит full-viewport layout-shift при переключении в fixed) → клип перенесён на внутреннюю обёртку __ribbon (как у S2 клипает frame) → CLS 0.0006. ПРАВИЛО: никогда не вешать overflow:hidden на элемент, который пинит ScrollTrigger
- Вторая находка (в процессе): поздний init пина S3 (fonts.ready-гейт из P3) вставлял pin-спейсер +220% во время скролла → возвращён init на mount; function-values + invalidateOnRefresh + дешёвый ScrollTrigger.refresh() на fonts.ready дают тот же точный центр (488/628 = ideal на 800/1080, spread 0) без сдвига
- Lighthouse mobile (devtools-метод): repeat (?nointro) Perf 96 / A11y 96 (LCP 1.6s!, CLS 0.002, TBT 130ms); first с прелоадером Perf 95 / A11y 96 (LCP 1.6s, TBT 80ms) — рост с V2 (94/88): hero-webp 68KB + srcset-preload. Единственный a11y-фейл — контраст белого на брендовом #F12625 (4.2:1, задокументированное решение за брендом)
- HANDOFF.md обновлён: приоритет доков R3, Lighthouse, CLS-правило, TODO заказчика пополнен R3-допущениями (12 карточек S4, направление ленты, диапазоны слайдеров)

## R3-P5 (S8 карусель + слайдеры S9 + форма S12) — ГОТОВ (2026-08-29)
- S8 (R3.8): пин = ровно вьюпорт (is-motion height 100svh, position relative), сцена — АБСОЛЮТНЫЙ оверлей inset 0 + @include container, align center; стек получил margin-top 48k (бокс на 48 выше карточки из-за теневой зоны) → АКТИВНАЯ КАРТОЧКА ровно в центре окна (инвариант скраба: центр стека = innerHeight/2 + 24k; замер 474/564/404 = expected на 900/1080/760). Арты: StepArt ПЕРЕПИСАН — 5 РАЗНЫХ каркасных рисунков по смыслу шагов (лупа над схемой узлов / чертёж с нодами / документ+подпись+печать / ракета / график вверх), один viewBox 220, сток 2 surface-2, АРТ ПО ЦЕНТРУ карточки (left/top 50% translate). Ховер-тилт ±5° за курсором ВНУТРИ карточки: rotateX/rotateY на внутреннем .step-card__tilt (gsap владеет трансформом самой карточки!), perspective 1000 на карточке, lerp 0.12, возврат в 0 на mouseleave (замер 3.02/3.48 → 0/0). Уход карточки: y −90, blur 10 (было −72/8, daoism-феел). daoism.systems headless не отдаётся (canvas) — сверка по refs/f4 + паттерну V2
- S9 (R3.9): PSlider ПЕРЕПИСАН на свободные непрерывные значения (step 0.1, БЕЗ options/ступеней): БАЗОВЫЙ СТЕЙТ ПО МАКЕТУ — красный «хвост» нарастает СЛЕВА к значению (трейл 280px), крещендо на значении, быстрый спад вправо (36px), серые края (снято попиксельно с ноды 887:51605); ховер зоны = крещендо +2px + мягкий red glow, drag = +4px + сильный glow; клавиатура = keyStepPct 2% за стрелку; РОЛЛИНГ значения: отображаемое число форматируется из ТОГО ЖЕ lerp'а visualPct, что и крещендо (едут синхронно). НОВЫЙ composables/useSliderFormat.js: money() EN $840/$87k/$1.2M, UA «840 ₴/87 тис ₴/1,2 млн ₴» (формат ₴/тыс/млн из UA-локали!), percent(), integer(). Калькулятор: margin/volume/aov = позиции 0..100, integrations 1..10 float (показ округлённый); диапазоны отображения локале-зависимые (VOLUME EN 5k..500k / UA 50 тис..12 млн ₴; AOV EN 50..2500 / UA 500..25 тис); ends = первые/последние подписи прежних степов; формула TODO(FAKE) без изменений, но на непрерывных позициях. Контакт-слайдер аналогично (EN 5k..150k / UA 50 тис..3 млн)
- S12 (R3.12): секция уже была близка к нодам — доведено 1в1: табы = две смежные скруглённые (r12) плашки с бордером, активная красная (border+text, z-index над стыком, margin −1px); дефолтные чипсы ПО НОДАМ (s1: Manually+Spreadsheets [0,2]; s2: Instagram [0]); БАГ: у инпутов оставался UA-дефолт border 2px inset с трёх сторон (CSS задавал только border-bottom) — «коробки» вместо подчёркиваний → border: none + border-bottom (замер 2/2/1/2 → 0/0/1/0). Футер: правая зона = колонка Additional + отдельный столбик (Back to top ↑ + EN/UA) как в макете, компания в две строки, text-align right; mockup-скрины нод 903:54647/903:53800 в скретчпаде (fig-s12a/b)
- Проверено: S8 скролл вниз-вверх (active 0→2→0, карточка исчезает/восстанавливается), тилт числами, 5 разных артов; слайдеры мышью (drag + роллинг «64%» в полёте), клавиатурой (5 стрелок = +10 = 2%×5, дисплей и результат едут), тач = нативный input; UA-формат ₴/тыс/млн live; форма оба таба до success (EN tab1, UA tab2 с валидацией); 390/768 без переполнений; консоль чистая; build 0 warnings

## R3-P4 (пакет мелких: S5/S6/S7/S10/S11) — ГОТОВ (2026-08-29)
- S5 (R3.5): фоновый знак top: calc(50% − 12px); мышиный параллакс ±8→±14 (обе оси, замер 13.81px у края); левая подпись БЕЗ капса (убран text-transform + модификатор --upper из шаблона; строка в локали sentence case)
- S6 (R3.6): иконки рядов IconMs :size 20 (в НОДЕ макета они 16×16 — CHANGES главнее); отступ заголовок↔описание gap 20→12 (визуально 16 ≈ макетные 20 box-to-box); ГЛАВНОЕ — обе карточки на ОДНОЙ subgrid-сетке: __cards grid 1fr 1fr, каждая карточка grid-template-rows: subgrid / grid-row: span 10 (тайтл/саб/точки/5 рядов/точки/кнопка), __header и __rows display: contents, ритм маргинами (sub 12, dots 32, row 16/первый 32, кнопка 32). Замер: rowDiffs [0,0,0,0,0], кнопки 0 — построчно идеально на 1440/1920 en/ua. md: __cards обратно flex-column, карточка grid-template-rows auto
- S7 (R3.7): все транзишены ховера 250ms ease → 350ms cubic-bezier(0.4,0,0.2,1) (bg строки, цвет имени/описания, поворот плюса); номер и заголовок выровнены ПО БАЗЛАЙНУ (align-self: baseline на обоих + lh 1 у номера; разница box-bottom −3px = разные descent'ы 20/40px шрифтов — это и есть выровненные базлайны, подтверждено скрином)
- S10 (R3.10): gap текстов 20→12; IconLine ПЕРЕПИСАН — 4 иконки (percent/growth/renew/balance) в одном квадратном viewBox 96×96, ТОЛЬКО цельный сток 1.5 (без пунктира), детальнее (циферблат-процент с красной диагональю, график с осями/точками и красной стрелкой, циклические стрелки с красной, весы с красным пивотом), стиль SVG_icon_example; появление 1→4: СВОЙ fromTo stagger 0.15 + y24 (v2-reveal с ячеек снят, pre-hide .earn-cell добавлен в main.scss в no-preference блок; mid-entrance замер [0.91, 0.75, 0.42, 0] — стагер виден); reduce — ячейки видимы статично
- S11 (R3.11): БАГ ОБРЕЗАНИЯ НАЙДЕН — margin-top: −12px у __content внутри panel-inner (overflow hidden) срезал верх первой строки ответа в открытом состоянии → маргин убран (замер contentTopInsideInner −12 → 0, нижний абзац +32 запаса, UA-длинные ответы целы); транзишен 320 cubic(0.22,1,0.36,1) → 380ms cubic-bezier(0.4,0,0.2,1) (симметричный ease-in-out в обе стороны)
- Проверено: числа+скрины на 1440 en/ua и 1920 en; 390/768 ua — 0 переполнений, compare стек 5 рядов, earn-ячейки доигрывают в 1; консоль чистая; build 0 warnings

## R3-P3 (S3 слои R3.3 + S4 карточки R3.4) — ГОТОВ (2026-08-29)
- Сверка с Figma: S3 opened 887:50019 — порядок слоёв стека в макете = наш (site,pay,delivery,analytics,crm,stock,notify, питч 89), НО лесенка подписей макета НЕ соответствует своим слоям (CRM-подпись стоит у слоя платежей и т.д.) → по R3.3.2 «поменять местами подписи»: стороны чередуются сверху вниз (Л:0,2,4,6 / П:1,3,5), каждая подпись у СВОЕГО слоя. S3 initial 887:49901 — фронтальная карточка 640×360 по центру. S4 887:50288 — карточки 416×225 r12 бордер, иконка 86×86 (пунктирный объект + красный плюс), текст 24, питч 448, каскад 0/0/32/64, лента с блидом −97, тик-полоса 1312×16 с крещендо-курсором; в кадре макета 4 карточки, ОБЕ крайние срезаны краями → трактовано как окно длинной ленты, взяты ВСЕ 12 позиций списка R3.4.3 (ДОПУЩЕНИЕ — сказать заказчику)
- R3.3.1 «не по центру и разваливается» — ДВЕ причины: (а) gsap.from с деком позицией CSS-трансформа (rotate+skewX+scaleY) давал кривой старт → явный fromTo {rotate 0, skewX 0, scale 1.54×1.54}↔{4,-44,1,0.72} + `.is-motion &__iso { transform: none }` (gsap владеет трансформом целиком); (б) цель коллапса — теперь ЦЕНТР зоны под головой: function-values + invalidateOnRefresh + офсеты через offsetTop-цепочку (transform-независимо) + `position: relative` на секции + ИНИЦИАЛИЗАЦИЯ ПОСЛЕ document.fonts.ready (стартовые значения from(), снятые до загрузки SF Pro, протухали — голова другой высоты) + ScrollTrigger.refresh() после init. Проверено: центр карточки = идеал ТОЧНО на 760/900/1080/1440 высотах и на 2560 (спред изо 0)
- ГРАБЛЯ ТЕСТОВ: во время пина secTop==0 на всём протяжении скраба — «доскроллить до secTop≈0» ловит случайную точку ВНУТРИ пина; правильная проба: выйти из пина вверх (top>30) и заползать шагами 20-120px
- R3.3.3: разъезд теперь СВЕРХУ ВНИЗ (stagger i*0.12, верхний слой первым) и подписи в том же порядке (Л,П,Л,П…) по прибытии своего слоя. R3.3.4: подписи absolute с top: calc(slotH/2 + slot*var(--pitch)) (--pitch 89k/73k static/motion на stage), внутренний __box translateY(-50%) (внешний — канал gsap); якорение проверено численно: dy=0 у всех 7 на 1440/1920/2560/760; lg-фолбэк — один вертикальный список в порядке слотов
- R3.4: ховеров НЕТ (is-lit/onEnter/glideTo/таймер/IO удалены), автопрогона НЕТ. Pin (100svh, контент по центру, +=250%) + горизонтальный скраб трека к −(scrollW−vw) + тик-полоса: заполнение и крещендо-курсор (svg 7 палочек, clamp 15px..100%-15px) на ТОЙ ЖЕ таймлайн через proxy-твин (сырой self.progress рассинхронивался со сглаженным scrub — 44.5% vs 49.5%); после фикса x/lit совпадают в каждой пробе (44.7=44.7, 55.1=55.1), реверс до нуля. НАПРАВЛЕНИЕ: лента едет ВЛЕВО при скролле вниз (взгляд идёт по списку слева направо; у produx живьём лента — декоративный параллакс ~0.16 БЕЗ пина, движется вправо; «слева направо» из CHANGES трактовано как направление просмотра — ДОПУЩЕНИЕ, флип = 1 строка). Блид −97 только в моушене; статика/reduce/мобила — нативный горизонтальный скролл ленты с паддингами контейнера (12-я карточка досягаема, проверено scrollLeft 4032)
- Локали: included.items = 12 новых строк R3.4.3 дословно (en/ua). Иконки: НОВЫЙ IconService.vue — 12 штук, один viewBox 86×86 (= блок макета; «напр. 48×48» в ТЗ — пример), сток 1.5, дэш 2 5, muted + фирменный красный плюс сверху-справа в каждой (сигнатура иконки макета); имена: store/catalog/crm/payment/delivery/stock/order/notify/analytics/admin/market/growth. IconLine остался для S10 (перерисовка в P4)
- Проверено: S3 старт = одна фронтальная карточка 644×3xx точно по центру (538=538 и т.д.), разворот сверху вниз, конец — все подписи op1 dy0; S4 скраб/реверс синхронны; 12 карточек, 12 разных иконок; reduce: S3 разложен с подписями (dy0), S4 скроллится нативно, пина нет; 390/768 ua/en — 0 переполнений; консоль чистая везде; build 0 warnings

## R3-P2 (hero R3.1 + секция проектов R3.2) — ГОТОВ (2026-08-29)
- Сверка с Figma: метаданные ноды S1 887:49936 (корзина x30 y88 w1288; H1-блок y272 h228, питч строк 138 при fs128; кнопки y708; заголовок ВЕРТИКАЛЬНО центрован между хедером 88 и кнопками); S2-финал 887:49994 = рамка 1312×569 ПО ЦЕНТРУ секции 815 (не вьюпорт-строук!); внутренности карточки next из полного узла 887:52811 (radial red glow + сетка мягких точек 5px/шаг 21 + тонкое кольцо-дуга + белый плюс). get_metadata на 887:52786 падает SSE-багом — читать use_figma-скриптом; strokeWeight/strokeDashPattern бывают Symbol/недоступны — try/catch
- R3.1.1: питч H1 176→136 (gap $spacing-9→$spacing-2 при lh 1.0; макет 138). R3.1.2: заголовок центрируется между хедером и кнопками (inner flex:1 + title margin-block auto; замеры 182/174 @1440, 318/310 @1920, 352/380 @2560)
- R3.1.3: корзина — геометрия макета точно (30/88/1288 @1440 = 2.08vw/6.11vw/89.44vw fluid); из Figma rawImages вытащен 4096×2272 прозрачный оригинал → hero-3d.webp 1600w (68KB) + НОВЫЙ hero-3d-2600.webp (125KB) + srcset/sizes + preload с imagesrcset (app.vue)
- R3.1.4: idle float ±10px (gsap y на img); мышь = ПОВОРОТ rotateX ±4°/rotateY ±6° на ОБЁРТКЕ __cart-wrap (perspective 1000px на __bg), lerp 0.07, mouseleave html → таргеты 0 (плавный возврат тем же lerp). Каналы не дерутся: gsap y на img, rotate на wrapper. md-центрирование обёртки margin-left −65vw (НЕ transform — его пишет тилт)
- R3.1.5: подпись hero заменена: EN «from launch to real sales» / UA «від запуску до реальних продажів» (locale hero_note)
- R3.1.6: ПОДЛОЖКА = text-shadow (rgba(10,10,11,.75) СВЕТЛЕЕ фона #070707) клипался overflow:hidden масок строк в жёсткий прямоугольник → убран text-shadow, вместо него filter: drop-shadow(2px 2px 64px …) на h1 (фильтр применяется ПОСЛЕ клипа масок — тень мягкая, бокса нет; параметры = DROP_SHADOW из ноды 887:49992). Маска SectionTitle: padding 0.08em сверху/0.16em снизу (+отрицательные маргины) — выносные (g y р у) и диакритика (й ї) целы, проверено зумами UA/EN
- R3.2.1: пин S2 переделан — БЕЗ фазы растягивания: is-motion секция ровно 100svh (padding 0, рамка по центру), пин top top, end +=150%, только горизонтальный скраб трека power1.inOut. Статика (<1025/reduce) — прежний стек
- R3.2.2: у рамки НЕТ своих бордеров; внутренние разделители ячеек остались; hairline-элементы top/bottom видны только на входе секции (gsap scrub top 45%→top top → opacity 0 к фиксации, реверсивно)
- R3.2.3: карточка «Your product could be next» пересобрана КОДОМ 1в1 по 887:52811: radial glow (66%/96% @52% 45%, red .18→.06→0) + точки background-image radial 21px + кольцо conic-gradient-дуга в mask-ring 1.5px (110px) + плюс svg 44 stroke 1.5
- R3.2.4: ховер карточек проектов — img scale 1.03 (было 1.06), название КРАСНЕЕТ color 200ms БЕЗ сдвига (translateX убран). R3.2.5: ховер next — кольцо крутится лоадером (animation 1.1s linear infinite, пока курсор на карточке), плюс красный; reduce — без вращения
- R3.2.6: сверено с живым trionn.com (скрин мид-скролла works: пин + горизонтальный трек + полновысотные ячейки с разделителями — наша модель совпадает; вход карточек clip-reveal из P6 = их паттерн)
- Ячейки: width 50cqw (container-type: inline-size на рамке) — ровно 2 на рамку на ЛЮБОЙ ширине (656 @1440 = макет 655, 860 @1920, 1145 @2560); cover aspect-ratio 590/414 вместо фикс-высоты; min-height 818 удалён (естественная высота = макет 567); мокапный next-project.webp больше не используется
- reduce на десктопе: рамка overflow-x auto — третья ячейка досягаема (страница полна без моушена)
- Проверено: питч/центровка/корзина/тень числами на 1440/1920/2560; тилт числами (±rotateX/Y, возврат); hairline 0.87→0 к пину; трек-скраб; ховеры числами (scale 1.03, имя красное, сдвиг 0.2px; кольцо крутится, плюс красный); round-trip наверх цел; 390/768 en+ua без переполнений; консоль чистая везде; build 0 warnings

## R3-P1 (большие десктопы 1920/2560 + кнопки + ритм) — ГОТОВ (2026-08-29)
- Исследование живого overdone.it (puppeteer + системный Chrome, ширины 1440–2560 бинарно): контейнер = ~95vw с капом 1790 (без max-width токена!); 1441–2559 типографика НЕ меняется вообще; единственный ап-брейкпоинт `@media (min-width: 2560px)` (2559→56px, 2560→78px у h2): hero 140→240, h2 56→78, h3 40→56, лиды/кнопки 20→27, кап контейнера ×4/3. Скейл НЕ через rem/zoom — рукописные значения. Всё в docs/BIG_DESKTOP.md
- Токены → CSS custom properties (_scale.scss, импорт в main.scss после theme): --fs-hero/h2/h3/h4/body-lg/body/small/nav/mono, --container-max (1720 база — флюидный рост как у основного сайта, 2290 на ≥2560), --rhythm-half (64→88), --btn-* (48/14/20/24/12 → 64/24/27/32/16), --k (1→1.4, множитель структурных ширин макета). SCSS-имена ($fs-h2…) сохранены и указывают на var() — КОМПОНЕНТЫ НЕ ТРОНУТЫ для типошкалы; -m мобильные токены остались px
- Кнопки: PButton переписан 1в1 с основного сайта (замеры hover live): h48 pill 52, pad 14, текст 20/400/ls normal, СТРЕЛКА СЛЕВА в покое; hover = левая схлопывается (width 24→0 + margin + fade), справа разворачивается «↗ в повёрнутой рамке» (их точные SVG-пути); ширина кнопки не меняется (проверено 232.72→232.72), фон/цвет не меняются НИ У ОДНОГО варианта, transition общий 0.4s, active — ничего. Варианты: primary красный / secondary gray (fill-soft) / white / ghost (легаси LangModal). Иконки схлопываются margin-техникой (не gap — gap не схлопнуть)
- Ритм: все секции на padding-block: var(--rhythm-half); хвосты contact (bottom 80) и footer выровнены; замеры content-to-content: 1440 → 122–134 (шум line-height), 2560 → 168–178 при цели 176
- Скейл секций на 2560 через --k: хедер (плашка 507×72, min-height 88, тикер), S3 сцена целиком (стек/слоты/изо/лесенки/подписи — была КОЛЛИЗИЯ с S4: гэп −29), S5 (min-height 768, statement 864), S4 карточки 416, S7 (name 520/desc 400), S8 (стек 640×432, карточка 640×384, арт, desc 414), S9/S12 панели 640, лиды 569/620/681 и «24px» из макета; hero: корзина left 2.08vw/top 6.11vw/width min(89.44vw, 2290px), min-height max(820px, 56.9vw); лого S5 calc(640*--k)
- Проверено: 0 переполнений и чистая консоль en+ua на 1440/1920/2560 (reduced full-page) и 390/768; пины S2/S3/S8 живут на 1920×1080, 2560×1440, 1440×900 (2 прохода, обратимо); ховер кнопок скриншотно + числово; build 0 warnings
- ЗАМЕЧЕНО для P2 (не чинил — там пункты R3.1): серая подложка за строками hero-заголовка видна на скринах (R3.1.6); hero-3d.webp 1600w мыльновата на 2560 (нужен ре-экспорт 2600w+ из Figma при правке позиции корзины R3.1.3, оригинал 4096 был в rawImages ноды 887:49936); синяя плашка в лого — НЕ баг, это настоящий лого клиента (assets/img/logo-overdone.svg)

## V2-P10 (полировка/QA/Lighthouse/HANDOFF) — ГОТОВ (2026-08-28)
- og-image: скрин hero 1200×630 → public/og-image.jpg + og:image/width/height меты в app.vue
- A11y-фиксы: aria-label на <p> statement запрещён (prohibited-attr) → visually-hidden span с полным текстом; табы формы aria-controls + role=tabpanel; тег карточки S8 muted→secondary (контраст 14px)
- ПЕРФ-НАХОДКА: SSR-HTML был 256KB, из них 153KB SVG — PSlider рендерил 960 тиков (5×192 rect) на сервере → тики теперь client-only (hydrated ref), SSR — CSS-заглушка той же ритмики; HTML 256→170KB. Плюс hero-3d.webp 2560w→1600w (116→60KB) + link preload + fetchpriority high (LCP-кандидат repeat-вью); nitro compressPublicAssets; тест-рычаг `?nointro=1` в pre-paint скрипте (QA/repeat-замеры)
- Lighthouse mobile (devtools-метод): БЫЛО 78/92 → СТАЛО: repeat (?nointro) Perf 94 / A11y 96 (LCP 2.7s, CLS 0.002, TBT 90ms); first с прелоадером Perf 88 / A11y 96 (LCP 3.2s — оверлей закрывает LCP, осознанная цена BUILD §S0). Единственный contrast-фейл — белый на брендовом красном 4.2:1 (задокументирован, решение за брендом)
- SSR-HTML не жмётся node-сервером — на проде обязателен gzip/brotli на инфре (в HANDOFF)
- HANDOFF.md переписан под V2 целиком: перенос, архитектура моушена, Lighthouse-методика, TODO заказчика (UA-REVIEW полный список, сабтайтлы S6, теги 03-05, ends S9, лейблы S6, скрины админки) и техники (API endpoint, формула, ASSET-3D, контраст, кейс-страница v1, bfcache)
- Регресс: слайдеры после hydration (5 lanes/192 тика/0 заглушек), клавиатура работает, полный проход без ошибок консоли, build 0 warnings

## V2-P9 (адаптивы 1280/1024/768/390 + пин S2) — ГОТОВ (2026-08-28)
- ДОДЕЛАН заказанный пин S2 (ответ №2 заказчика): mm(≥1025+no-pref) pin +180% scrub: фаза 1 (0–0.35) рамка/строук растягивается height→max(base, vh−96) (clamp — на низких экранах не сжимается), фаза 2 (0.3–1) трек x→−(scrollW−frameW) power1.inOut; invalidateOnRefresh, cleanup clearProps; ячейки height 100%. <1025 — вертикальный стек ячеек (border-top вместо left)
- Токены: $fs-hero-m 56 (+sm ступень 40px в hero); S4 glideTo меряет РЕАЛЬНЫЙ pitch (offsetLeft разница — карточки 300px на md)
- respond-блоки по всем секциям/футеру: lg: головы в колонку (lead full), calc/contact в столбец (панели full), earn 2×2 (восстановление схлопнутых бордеров nth-child), layers labels → статичный список под стопкой (лесенка-выноски скрыты), scope name flex1/desc 300; md: заголовки всех секций $fs-h2-m, hero cart 130vw центр, projects cover 46vw + мета колонкой, layers компакт-стек (slot 120/mb −58/iso 270), model statement h3-m + лого 400, steps карточка full + art вправо, faq/compare/calc паддинги, contact: поля full, чипы min-height 44, каналы 120, футер wrap + legal колонкой; sm: earn 1 колонка
- ФИКСЫ по скану: steps texts на 1280 (desc width auto/flex1 в пин-режиме ≤xl), scope на 1024 (name flex1 min-width 0), guard в SectionSteps nextTick (!stackEl → return; race при мгновенном reload во время mount — PAGEERROR null)
- Проверено: 8 комбинаций (ua/en × 1280/1024/768/390) полный скролл — 0 переполнений, 0 ошибок; S2 пин на 1440 (рамка тянется, трек −589); тач-таргеты 390: chip 47/tab 77/submit 48/faq 80/slider 48 (все ≥44); build 0 warnings

## V2-P8 (моушен S5/S7/S9–S11) — ГОТОВ (2026-08-28)
- S5 (SectionModel): свой сплиттер строк с [акцентами] на span-слова (2 строки раздельно, aria-label исходной строкой, слова aria-hidden), scrub top 78%→bottom 42%, onUpdate classList.toggle('is-on') (НЕ Vue :class), CSS: дефолт=финал (белый/красный), .is-anim → muted→on; подписи+divider fade+up на половине (отдельный trigger center 55% once, v2-reveal с них снят — у секции свой моушен, sectionReveal не вызывается); лого: CSS rotate 40s linear + mouse-параллакс lerp .06 ±8 (канал `translate`); watch(plain)→nextTick(applyLit) на смену локали
- S7 (SectionScope): elementFromPoint-модель из R2 (mouseX/Y + rAF-throttle + resolve на mousemove/lenis scroll/window scroll), :hover заменён классом .is-active; тач: pinned tap-toggle (первая открыта). ЗАМЕТКА: BUILD просил grid-rows раскрытие desc, но в МАКЕТЕ desc всегда в потоке (transparent→white) — оставлена вёрстка макета, высота строки стабильна
- S9: PSlider — visualPct с gsap-лерпом 0.3s power2.out (тики едут плавно за значением), drag (pointerdown) → крещендо +4px и glow drop-shadow на лейне (input:active ~ lane), viewBox 20px под подросшие тики; SectionCalc — rolling-каунтер результата (gsap snap 1, 350ms, tabular-nums), reduce — прямое присваивание
- S10: стагер слева направо уже реализован sectionReveal-ом (DOM-порядок ячеек, stagger 0.08) — отдельного кода не потребовалось
- S11 (SectionFaq): панель grid-rows 0fr→1fr 320ms (без замеров), aria-hidden на закрытых, max 1 открыт сохранён
- Проверено: S5 слова 0→19/29 по scrub + подписи на половине + спин лого; S7 активная строка 1→3→5 при скролле под НЕПОДВИЖНЫМ курсором; S9 каунтер 6–8→5–7 с роллом; S11 высота 47→68 в анимации, свап плашек (max 1); reduce по всем пяти: S5 белый сразу/спин off, S7 без транзишнов, S10 видимы, S11 0s; консоль чистая, build 0 warnings

## V2-P7 (pin-секции S3 + S8) — ГОТОВ (2026-08-28)
- S3 (SectionLayers): дефолт DOM/CSS = РАЗЛОЖЕННЫЙ стейт (= mobile/reduce фолбэк); mm(≥1025+no-pref): classList.add('is-motion') СИНХРОННО до замеров (R2-P3 грабля) → компактная раскладка (mb −112, labels gap 96) → pin 'top top' +220% scrub 1: фаза A (0–1.2) iso-обёртки from {rotate 0, skew 0, scaleY 1, scale 1.54=640px фронтал}, фаза B слоты from y=collapse_i (все к центру стопки, глубокий едет первым, stagger 0.12, power1.inOut), подписи from autoAlpha 0 по прибытии своего слоя (at+1.1), hold-хвост 0.5. Реверсивно (spread 0→190→438→10 при откате)
- S8 (SectionSteps): motionMode v-if (карусель) / статичный вертикальный список карточка+текст (v2-reveal); mm: стопка absolute (глубина ≤2 видимых: y 16/шт, scale −0.04, alpha 1/.65/.3), pin +250% scrub 1, сегмент s: карточка уходит y −72 scale .92 autoAlpha 0 FILTER BLUR 8px (filter анимируется gsap), задние подтягиваются; текст справа: все 5 в grid 1/1, is-active по floor(progress·5), фейд 250ms; hover активной: CSS `translate: 0 −6px` (отдельный канал — не дерётся с gsap transform) + бордер; watch(items) → mm.revert+init (локаль)
- Проверено: S3 старт собран (spread 0, подписи 0) → mid разъезд (190) → конец разложен (438, подписи 1) → откат собирается (10); S8 стопка 11100→00111→00001→01110 (реверс), текст синхронен (0→2→4→1), blur 8px на уходящей; 2 полных прохода страницы вверх-вниз — без дыр/рассинхрона, hero цел; reduce: 0 пинов, S3 разложен с подписями, S8 список 5 рядов; 768: то же; консоль чистая, build 0 warnings

## V2-P6 (референсные анимации S2 trionn + S4 produx) — ГОТОВ (2026-08-28)
- S2 (SectionProjects): свой вход вместо v2-reveal на ячейках — covers clip-reveal снизу (inset(100% 0 0 0)→0 + y40, 1.15s expo.out, stagger 0.15) + img внутри scale 1.15→1 (clearProps transform — безопасно, CSS pre-hide на img нет) + меты fade+up (0.35 offset). Pre-hide в main.scss: html.js .project-cell__cover clip-path + __meta opacity (no-preference). Hover: img scale 1.06 800ms cubic-bezier(0.25,0.46,0.45,0.94), имя translateX 8px; reduce: всё статично
- S4 (SectionIncluded): автопрогон STEP_MS 2500 — activeIdx по кругу (11), тик-полоса lit width (i+1)/11 с transition 700ms, лента glideTo: gsap.to(track, x=-clamp(i*448−448, 0..maxShift), 0.8s power3.out, overwrite auto); карточка .is-lit (border white .45 + bg white .04, transition 350ms); hover: перехват подсветки + пауза (трек при hover НЕ едет — карточка не уезжает из-под курсора); IntersectionObserver threshold .3 — вне экрана пауза; reduce: полоса display none, прогона нет, is-lit визуально нейтрализован
- Проверено: S2 клип анимируется (8.5% mid-flight→0), меты доезжают, hover-зум 1.06; S4 прогон active 1→2→3 + полоса 18→27% + трек 0→−448→−896, hover держит подсветку 2.8s+ и трек стоит; reduce: clip none/meta 1/ticks none/трек none; консоль чистая, build 0 warnings

## V2-P5 (прелоадер + глобальные появления/ховеры) — ГОТОВ (2026-08-28)
- AppPreloader переписан (S0): html.od-preload pre-paint (скрипт nuxt.config прежний), фигура = loader-figure.svg 640 (rotate 14s linear + breathe scale 6s через INDIVIDUAL CSS-свойства rotate/scale — не конфликтуют с transform входа), проценты 96px слева-снизу, полоса 2px red. Прогресс: rAF, реальная загрузка (fonts.ready + hero-3d decode), кривая: до готовности ползёт к 90% за 2.5s, после — к 100% не раньше 1.2s; финиш: is-leaving фейд 650ms, session-флаг, класс снят, нода удалена; гейт hero через useState('od-preloader-active')
- SectionTitle: внутренний span.section-title__line + overflow hidden на компоненте (маска для ВСЕХ заголовков; паддинг-компенсация дескендеров 0.12em при lh 1)
- useScrollMotion.sectionReveal(root): строки .v2-mask .section-title__line yPercent 110→0 (1.05s expo.out stagger 0.1) + элементы .v2-reveal y24/autoAlpha (0.9s power3.out stagger 0.08), один timeline, trigger top 78% once. CSS pre-hide в main.scss под html.js + no-preference (SSR не мигает). ДВЕ ГРАБЛИ: (1) clearProps возвращает CSS pre-hide (как R2-P4) — конечные inline-значения ОСТАВЛЯЮТСЯ; (2) gsap затягивает CSS translateY(110%) в свой px-кэш `y` и ВОЗВРАЩАЕТ его после yPercent-твина — в from обязательно {yPercent:110, y:0}
- Все 11 секций S2–S12 подключены: классы v2-mask/v2-reveal + ref+sectionReveal (hero исключён из CSS pre-hide — LCP-safe: его каскад только за прелоадером)
- SectionHero: каскад за прелоадером (gsap.set скрытие пока оверлей закрыл → watch гейта → корзина autoAlpha/scale 1.04→1 + строки из масок + суб-ряд), idle float ±6px (gsap y, yoyo) НЕЗАВИСИМО от параллакса (параллакс пишет CSS `translate`, float — transform y: разные каналы), mouse-параллакс lerp 0.07 ±14/±10 только (hover:hover)
- PHeader: маркиза nav-строки — 2 группы + translateX(-50%) 20s linear infinite (reduce: нет)
- Проверено: свежий визит (проценты 22→72→100, выход, hero-каскад доигран), секции ниже фолда скрыты до входа и раскрываются, маркиза едет, повторный визит без прелоадера и статичный hero, reduce: 0 пинов/49 reveal-элементов видимы/12 строк без сдвига, консоль чистая, build 0 warnings

## V2-P4 (статика S7–S12) — ГОТОВ (2026-08-28)
- SectionScope (S7): строки border-table p32: имя 40 muted w520 + desc 20 (transparent→white) w400 + плюс 24; :hover = bg white .07, красный номер w32 появляется слева, плюс→крест. Тайтлы v2.scope_titles (EN дословно из макета), descs из scope.items
- SectionSteps (S8): проп step (0..4): карточка 640×384 surface radius 12 p40 («01 / 05» 24 Medium red + «/ 05» 16 red .5, тег-пилюля v2.steps_tags, заголовок 40) + StepArt.vue (вложенные wireframe iso-кубы, stroke surface-2, параметрическая геометрия, вариации по шагу) + справа линия 80 + desc w414. Теги 01 observation / 02 planning из макета, 03-05 ПРИДУМАНЫ: agreement/development/growth
- PSlider ПЕРЕПИСАН под макет S9/S12: лейн 192 тиков (1px, шаг 3px, h8; у позиции крещендо 16/12px), окраска красная у значения с фейдом в серый (radius 160px), label слева/значение справа над лейном, ends снизу; нативный input сохранён (grab-зона 44px, focus-ring)
- SectionCalc (S9): слева title/lead/8 чипов (v2.calc_chips по макету), справа панель 640 (bg black .25 radius 12): «A rough estimate», 4 слайдера (margin/volume5/aov/integrations 1-10), результат «X–Y%» 40 + suffix, disclaimer. Формула legacy estimateRange (TODO(FAKE)), адаптирована к ступеням. ENDS слайдеров в макете — заглушки ($10k у Margins) → поставлены смысловые (Low..High, Under $100..$2k+, 1..10); volume EN = 5 ступеней $ (для формулы)
- SectionEarn (S10): head (align.title + v2.earn_lead_1/2) + 4 ячейки-таблица h328 p32: номер red Medium, title 24, desc 16 muted (НОВЫЕ тексты v2.earn_cards из BUILD_V2 дословно, обе локали), IconLine 130 (percent/growth/renew/balance — добавлены в IconLine + size prop). Прозрачность-каскад макета (1/.75/.5/.25) = кадр reveal-анимации → статика все 1, edge-fade не переносён
- SectionFaq (S11): title «FAQ», плашки gap 4 p32: closed bg white .03 вопрос 32 muted; open bg .07 вопрос red + ответ 20 white (абзацы по \n\n); hover — вопрос/плюс белые; плюс→крест; max 1 открыт (toggle работает, плавная высота в P8)
- SectionContact (S12): фон form-3d.webp (bottom −33, left 62%), слева title/intro (v2.contact_p вариант 1) + блок Book a Call/Telegram (2 ряда h168, glass black .5 blur 64, лейблы 32 muted, иконки Material 32); справа панель формы 640 (glass black .5 blur64 border-table radius 12): табы (рамка radius 8, активный красный inset-бордер + текст, иконки add/remove_shopping_cart в IconMs), FLOATING labels (не placeholder-only — правило видимых label из CLAUDE; звёздочка красная), 4 поля (row1 два half), чипы (multi, is-on: белый бордер), слайдер v2.volume_steps5, submit primary block loading. Валидация legacy: required + URL-чек (link, только таб has), ошибки под полем красным, фокус первого невалидного, mock success (form.success_*). Проверено: смена таба меняет поля, пустой submit → 3 ошибки
- PFooter ПЕРЕПИСАН: колонки Social networks/Pages/Services (E-commerce Build → «/», Recruiting) + правый блок Additional/Back to top/EN-UA свитчер; legal: mono @2026 OVERDONE + company·number·address. «Recruting» (опечатка макета) исправлена; «@2024» макета → оставлен наш @2026
- Проверено: build 0 warnings, обе локали полный проход без переполнений/ошибок, скрин-сверка S7–S12 (S11 в т.ч. open, S12 оба таба)

## V2-P3 (статика S3–S6) — ГОТОВ (2026-08-28)
- ui/LayerCard.vue: 7 наших v3-векторов 640×360 инлайн (site/pay/delivery/analytics/crm/stock/notify), цвета через var(--color-*)
- SectionLayers (S3): два стейта по пропу opened (дефолт сложенный: одна карточка site 640 фронтально; открытый: изометрическая стопка transform rotate(4deg) skewX(-44deg) scaleY(0.72), карточка 415px, слоты 587×185 с mb −96, z 7..1, лесенка подписей: слева site/pay/delivery/analytics (top 250, gap 112), справа crm/stock/notify (top 296)); подписи 24/20 muted, линии-выноски 64px
- ui/IconLine.vue: 11 иконок 86×86 для S4 в стиле refs/SVG_icon_example (dashed stroke 1.5 muted + красный акцент); ui/IconMs.vue: 10 глифов Material Symbols Outlined wght300 (payments/receipt_long/shield/warning/target/trending_up/build/autorenew/alt_route/done_all, viewBox 0 -960 960 960, fill currentColor)
- SectionIncluded (S4): двухколонная голова (title v2.included_title pre-line + included.lead 24px), тик-полоса 16px (repeating-linear-gradient 2px/8px, lit-сегмент 9% статично — автопрогон в P6), full-bleed лента: track margin-left −97, карточки 416 radius 12 border-table, каскад top [0,0,32,64], edge-фейды 64px
- SectionModel (S5): лого logo-mark-big.svg 640 абс за текстом, statement 40px две строки (l1 right / l2 left, красные [no upfront fee,] и [percentage of sales after launch.] через SectionTitle-спаны, aria-label целиком), divider border-table, 2 подписи 20 muted (левая uppercase; тексты из BUILD_V2 §S5)
- SectionCompare (S6): head title+lead 24 w569; 2 карточки flex 1 gap 32 p32 radius 12 blur16 (classic: bg white .05 border-table; partner: bg red .05 border red .25); заголовок-ряд: марка 2×2 кружков 12px (partner — красная, rotate −45) + title 28 (+badge пилюля у partner), sub 20 muted, точечные разделители (repeating-gradient 2/4px currentColor .25), 5 строк: IconMs 16 (цвет = color карточки: muted/accent) + label secondary + value white Medium; кнопки block: classic secondary «How it works»→#layers, partner primary cta.discuss→#contact
- Тексты: v2.* дополнен (layers_p1/p2, layers.{7 слоёв}, included_title, statement_l1/l2, statement_cap_left/right из BUILD_V2, compare_lead/rows/titles/subs). EN дословно из макета; labels строк в макете разнобойные между карточками (Motivation of the performer vs Executor motivation) — взяты по левой карточке единообразно. Сабтайтлы карточек S6 в макете — заглушка («Orders are immediately sent to the manager» в обеих) — поставлены по смыслу: classic_sub/partner_sub, ЖДУТ ПОДТВЕРЖДЕНИЯ заказчика
- TODO(UA-REVIEW) добавились: layers_p1/p2, имена/описания 7 слоёв (v3-тексты), statement_l1/l2, statement_cap_left/right, compare_lead, classic_sub/partner_sub
- Проверено: build 0 warnings, обе локали 1440 без переполнений/ошибок консоли, скрин-сверка S3(оба стейта)/S4/S5/S6 с нодами макета

## V2-P2 (статика: шапка, S1 Hero, S2 Проекты) — ГОТОВ (2026-08-28)
- Ответы заказчика в FIGMA_MAP: формы — правые s1/s2; S2 — стартовый стейт, при скролле pin + горизонтальный скролл + строук карточки тянется на всю высоту (P6/P7); S8 intermediates — ориентир, анимация с daoism; FAQ — «Section 10 - FAQ»
- Токены: $fs-hero 128 / $fs-h4 32 / $fs-nav 15 (_variables), --color-border-table rgba(161,163,165,.25) / --color-fill-soft rgba(255,255,255,.1) (_theme)
- Локали: новый блок v2.* в обеих (nav_ticker, hero_title_l1/l2, hero_note, projects_title, paparazzi_desc, website, details, next_title, next_desc, discuss_short) + cta.how; переносы строк заголовков — «\n» в локали + white-space:pre-line (у S2 title accent наследует, т.к. глобальный nowrap акцентов мешал макетному переносу «growing / with us»)
- PButton: текст 20/-3%, primary pl16/pr12 + икона 24 белый круг/красная стрелка, secondary row-reverse (икона слева) bg fill-soft; PHeader: лого 50px, центр-плашка 507×72 radius 8 (ссылки 15px + разделитель + статичная mono-строка nav_ticker с mask-градиентом по краям; Contact → goToContact), CTA Book a call primary; бургер/моб-меню как были
- SectionHero (новый): корзина hero-3d.webp abs (left 30, top 88, 1288w), виньетка градиент, H1 128 из двух SectionTitle span-строк (l1 слева, l2 справа), низ: кнопки + note 32px secondary; SectionProjects (новый): рамка border-table, трек 3 ячейки 654×818 (title/paparazzi/next), обложки 414px radius 4, мета имя 32 + desc 20 muted + кнопки Website/Details / Discuss project; overflow hidden (гориз. трек для будущего пина)
- nuxt.config: components ignore ['**/_legacy/**'] — иначе NUXT_B3011 коллизии имён с legacy
- UA hero легла в 3 строки (128px не вмещает «Система онлайн-продажів» в одну) — выглядит гармонично, ждёт вычитки
- TODO(UA-REVIEW): v2.hero_title_l1 «Система [онлайн-продажів]», hero_title_l2 «[Без оплати] на старті», hero_note «від ідеї до готового продукту», projects_title «Бізнеси, що зростають / [разом з нами]», website «Сайт», details «Деталі», next_desc «Зв’яжімося та обговорімо деталі», cta.how «Як це працює»
- Проверено: build 0 warnings, обе локали 1440 без переполнений, консоль чистая, скрин-сверка hero/S2 с нодами макета — совпадение по композиции/типографике/акцентам

## V2-P1 (ревизия + карта макета) — ГОТОВ (2026-08-28)
- BUILD_V2.md и PROMPTS_V2.md ГЛАВНЕЕ PAGE_SPEC/CHANGES_R2 (страница пересобрана в Figma). Файлы + refs/ (f1-f4.png, SVG_icon_example.svg) распакованы из od-build-v2.zip в корень проекта
- Карта макета → docs/FIGMA_MAP.md: корень «E-commerce page sections - ready» 887:48938, правый столбец x=5020 = финалы. ВАЖНО: имена «Section 9/10» в макете сбиты — S9 калькулятор = 887:51605, S10 We earn = 887:50770 (сверено по содержимому)
- Старые секции → components/_legacy/sections/ (13 шт), index.vue — заглушка с PHeader/PFooter/LangModal; ui/layout/локали/composables нетронуты; build чистый; кейс-страница /projects/paparazzi осталась как была
- Ассеты: public/img/v2/hero-3d.webp (корзина, ПРОЗРАЧНАЯ — брать rawImages 4096px, export-нода отдаёт запечённый чёрный!), form-3d.webp (прозрачный), assets/icons/loader-figure.svg = logo-mark-big.svg (один вектор). Иконки секций — при вёрстке P3/P4
- Figma MCP: metadata корня ломает SSE (слишком большой) — карту детей брать через use_figma query; скриншоты нод: max ~5 изображений на один вызов
- Дизайнер использовал наши наработки: v3-карточки слоёв (тексты варианта А в EN), statement вариант 3, contact-интро вариант 1, футер «E-commerce Build»

## Figma-макет 1440 (2026-08-26) — ГОТОВ
Пользователь: «даже близко не похоже» на живой лендинг → попросил статичный макет 1440 со всеми конечными/открытыми состояниями в Figma-файле <OD> Overdone.it, страница «Page » (node 826-2681). Собран фрейм «Partnership Landing — 1440 UA» (root 828:2325, высота ~11265) через официальный Figma MCP (use_figma + upload_assets; console-мост Desktop Bridge был не подключён). Шрифты: SF Pro Display у пользователя НЕ установлен — макет набран системным «SF Pro» + IBM Plex Mono. Состояния: included — карточка «сервіси доставки» подсвечена + прогресс ~63%; steps — вид пина, шаг 01, стопка+точки; scope — ВСЕ строки раскрыты (num красный, ×); FAQ — ВСЕ плашки открыты; model — весь текст белый, фраза красная; форма — таб «Вже є магазин», чипы CRM/Таблиці выбраны. Обложка Paparazzi — реальный og (webp Figma НЕ отрендерила «Could not read image size» → перезалит JPEG). Грабли MCP: resize() после layoutSizingHorizontal='FILL' сбрасывает в FIXED; изометрия слоёв — relativeTransform [[0.866,-0.866,tx],[0.5,0.5,ty]]; скриншот секции с fills=[] прозрачный (белый текст «исчезает» — не баг)

## Круг R2 — источники
Спека круга: prompts/CHANGES_R2.md (ПРИОРИТЕТНЕЕ PAGE_SPEC), промты: prompts/PROMPT_R2.md, фото r2-s1…r2-s18 в родительской папке «Overdone site/». Решения заказчика: лого рендерить как в файле (красный круг + СИНЯЯ полоса + белый текст), новый логотип везде включая прелоадер; скрины кейса — инструментом с лучшим результатом (puppeteer-core + системный Chrome).

## R2-P6 (steps daoism + align sharplink + model trionn + FAQ) — ГОТОВ, круг R2 закрыт
- SectionSteps переписан (R2.10, r2-s16 daoism): pin +250% scrub 1, слева стопка карточек шагов (номер «01 / 05» mono сверху, заголовок снизу), верхняя перелистывается по scrub (yPercent −46, rotate −7, fade), задние подтягиваются на слот (y +16/шт, scale −0.04, alpha −0.18); справа текст шага (desc+desc_2) в grid-стеке 1/1 с кросс-фейдом 250ms по active=floor(progress·5); прогресс-точки под стопкой. Обратный скролл отматывает (один timeline). Карточкам стека НУЖЕН НЕПРОЗРАЧНЫЙ фон (surface, без blur) — сквозь glass-подложку читались заголовки задних. Статика (мобайл/reduce) — v-if-ветки, не CSS: вертикальный список glass-карточек с desc внутри (нет дублей текста для скринридера). Смена локали пересоздаёт mm целиком (watch items → mm.revert() → nextTick(initMotion)) — иначе Vue-ререндер стирает gsap-трансформы стека
- SectionAlign переписан (R2.12, r2-s17 sharplink): грид 0.9/1.1 — слева title+intro сверху и outro прижат книзу (margin-top auto), справа 4 строки с пунктирными разделителями: mono-номер, текст body-lg, пунктирный line-art 120×96 (щит+пульс, чек-флоу, шестерни, корзина+плюс; stroke 1.5, muted + красный акцент). Старая колода удалена
- SectionModel переписан (R2.13, r2-s18 trionn about): только model.statement (новые ключи в локалях + model.statement_key для красной фразы — тексты не хардкодятся в компоненте), свой сплиттер на span-слова (aria-label исходной строкой на <p>, span-ы aria-hidden), scrub top 80%→bottom 45%, onUpdate пишет .is-on через classList (НЕ через :class — Vue сотрёт), watch(words) переприменяет после смены локали. CSS: дефолт = финал (белый, ключ красный) — reduce/no-JS сразу белый; .is-anim переводит в muted→is-on. Старые p1–p5/график не выводятся (ключи в локалях остались)
- FAQ (R2.6, r2-s11 sharplink): PAccordionItem = glass-плашка radius-lg, закрытая с тихим бордером (border, не strong!), открытая — border-strong; плюс в круге 40px с бордером (открыто: rotate 45 + красный); паддинги $spacing-6/7; список: gap 16px, на всю ширину контейнера (max-width 840 снят)
- ФИНАЛЬНЫЙ ПРОХОД КРУГА: обе локали × 1440/1024/768/390 — прокрутка всей страницы, ноль переполнений, ноль ошибок консоли; model дожигается до 38/38 (UA) и 47/47 (EN) слов; мобайл steps: 5 карточек+6 desc, без пина; reduce: 0 пинов, model сразу белый, steps статичны; два пина сосуществуют, спейсеры пересчитываются на resize (3190/5318 → 2840/4756), страница докручивается до конца. Скрипты: scratchpad r2p6-check.js, r2p6-final.js

## R2-P5 (проекты trionn + кейс Paparazzi) — ГОТОВ
- Ассеты: og-image магазина скачан → public/img/partners/paparazzi-og.webp (sharp, 1200×630, 62KB; sharp стоит в scratchpad, НЕ в проекте); обложка «наступним» сгенерена кодом (SVG→sharp→webp: фон #070707, пунктирная сетка с radial-fade, красный glow, тонкий «+», пунктирное кольцо) → public/img/partners/next-project.webp (11KB); скрины pprz-school.overdone.it → public/img/case-paparazzi/{home,catalog,product}.webp (puppeteer 1440×900, скриншот сразу type:'webp'). Тёмная тема магазина: localStorage['paparazzi-theme']='dark' + document.documentElement.dataset.theme='dark' (+reload); локаль UA: cookie i18n_redirected=uk-UA. Для product-страницы товар выбран с реальным фото (первый в каталоге — с заглушкой-иконкой)
- SectionProjects переписан (R2.8, r2-s13 trionn): грид 2×minmax (lg: 1 кол), карточка = обложка в рамке (radius lg, aspect 1200/630, overflow hidden) + мета-ряд под ней (имя+desc слева, mono-ссылки справа: «Дивитися кейс»→/projects/paparazzi (NuxtLink), «Відвідати сайт»→paparazzi.shop; у next-карточки «Обговорити проєкт»→#contact). Hover: cover scale 1→1.06 600ms cubic-bezier(0.25,0.46,0.45,0.94), имя краснеет, стрелка едет. Появление: fromTo y64 + rotation ±1.6 (на мобиле ≤768 rotation 0 — та же грабля расширения scroll-области, что у compare-spread), карточки БЕЗ .reveal-класса (грабля P4). Старые заглушки paparazzi-placeholder.jpg/paparazzi-logo.svg удалены, HANDOFF обновлён
- pages/projects/paparazzi.vue (роут /projects/paparazzi): hero (eyebrow CASE STUDY → h1 → tagline → mono-ссылка на сайт → обложка), абзац about крупно, 5 скринов по одному в ряд в браузерных рамках с mono-подписями (3 реальных + 2 плейсхолдера админки с TODO(USER) в комменте кода), CTA внизу (projects.next_title + primary → navigateTo('/#contact')). Тексты case_pprz.* дословно из CHANGES §R2.8 в обе локали. Шапка/футер/LangModal подключены; useHead title «Paparazzi — Overdone»
- Кросс-страничная навигация: PHeader.goToContact — если #contact нет на странице → navigateTo('/', hash '#contact'); pages/index.vue onMounted при наличии hash ждёт 600ms (пины создают спейсеры) → ScrollTrigger.refresh() + scrollTo(hash); обе страницы в onBeforeUnmount убивают ВСЕ ScrollTrigger-ы (порядок жизненного цикла: старая страница unmount → новая mounted, поэтому безопасно); кейс-страница на mount делает lenis.scrollTo(0, immediate)
- Проверено: карточки (2, обложки, hover 1.06, ссылки), клик→кейс (SPA, скролл 0), 5 подписей дословно, CTA и хедер-Контакт с кейса → лендинг с #contact во вьюпорте (top=0), пин layers жив после round-trip (спейсер на месте), UA/EN, 390 без h-переполнений, reduced-motion кейс полностью виден, консоль/pageerrors чистые, билд чистый. Скрипты: scratchpad pprz-shots.js, r2p5-check.js, r2p5-final.js

## R2-P4 (included produx + compare orionix) — ГОТОВ
- SectionIncluded переписан (R2.5, r2-s8/s9/s10): новый заголовок included.title «Уся технічна частина — [на нашому боці]» + included.lead в обеих локалях (старый сохранён как title_old; intro_1..3/outro в локалях, но не выводятся), шапка в 2 колонки, 11 крупных glass-карточек (иконка 32 сверху / имя снизу, min-height 150/120), produx-автопрогон: подсветка бежит по карточкам каждые 2200ms + тонкий красный прогресс-бар под гридом (width transition 2200ms linear, .is-snap на сбросе цикла/hover). Hover перехватывает подсветку и ставит прогон на паузу; IntersectionObserver (0.25) паузит вне экрана; reduced-motion — без автоплея
- SectionCompare переписан (R2.9, r2-s15): две прайсинг-карточки рядом (grid 2×minmax, max-width 1080), строки «критерий: значение» с нейтральным минусом (classic) vs красным чеком (partner), партнёрская акцентная: бордер rgba(241,38,37,.45)+glow, бейдж «Наша модель», PButton primary block → #contact (у classic CTA нет). Мобайл: стек, партнёрская первой (order:-1). Появление fromTo y48+x±spread со stagger 120ms
- ДВЕ ГРАБЛИ: (1) НЕ вешать класс `.reveal` на элементы с динамическим `:class` — Vue-патч класса стирает is-revealed, и после clearProps:'transform' CSS-сдвиг .reveal (y24) ВОЗВРАЩАЕТСЯ (карточки 0–1 повисали на +24px после первого тика автопрогона); pre-hide делают inline from-значения твина (immediateRender), .reveal-класс не нужен. (2) Горизонтальный x-офсет входа на мобиле = h-скролл всей страницы ещё ДО срабатывания триггера (immediateRender ставит x:+32 сразу) — spread обнулять через matchMedia(max-width:768)
- SectionTitle: `white-space: nowrap` на акценте (рвался «on / us»); проверено — все акценты обеих локалей влезают на 390 (макс «початкового бюджету» 330px < 358)
- Проверено: автопрогон бежит (бар 18%→27%), hover держит idx во время паузы, 3 ровных ряда грида после тиков+hover-цикла (transform none у всех), compare 2 колонки/бейдж/1 CTA/5 чеков, UA 390 без h-переполнения (offender-скан: 0), билд чистый. Скрипты: scratchpad r2p4-check.js, r2p4-regrid.js, r2p4-overflow2.js

## R2-P3 (слои neoconda) — ГОТОВ
- SectionLayers переписан (R2.4, фазы s5→s6→s7): шапка секции в 2 колонки (заголовок слева, справа p1 + акцентный p4; p2/p3 не выводятся), крупная центральная сцена (.lscene, plane-w 420/400-в-пине). Каждый слой — СВОЯ inline-SVG графика (CRM канбан, карта+чек, ячейки склада, маршрут с пином, пузыри сообщений, бар-чарт; stroke 1.5 + surface-2 + красные акценты). Подписи ЛЕСЕНКОЙ со сменой сторон с выносками, появление opacity-only ПО ПРИБЫТИИ своего слоя. Closed: слои подоткнуты под мокап (30px + рёбра 12px) — единый объект; разъезд по scrub, stagger 0.9, rotate(-3) на РЯДЕ; hold-хвост 1.3 (сборка к ~85% пина). Статика (мобайл/reduce): CSS-финал + легенда списком
- ДВЕ ГРАБЛИ: (1) НЕ анимировать rotation на элементе с CSS 3D-transform — gsap переписывает весь transform, изометрия разваливается; вешать на плоский контейнер. (2) При компактных CSS-переменных под .is-motion — classList.add СИНХРОННО до замера offsetTop (Vue-биндинг на nextTick — цели коллапса считались по старой сетке)
- Замер пин-фаз: wheelTo с settle-and-correct раундами (инерция Lenis перелетает). Проверено: closed 0.03 / mid 0.5 / open 0.85 = s5/s6/s7, стек 815<900, мобайл UA без переполнений, билд чистый

## R2-P2 (hero spur + scope thewatch) — ГОТОВ
- SectionHero переписан (R2.7): центрированная колонка eyebrow → H1 (clamp 42–68px, 2 строки в ОБЕИХ локалях — размер подобран под UA) → новый короткий суб hero.lead (старый текст сохранён в hero.lead_full) → кнопки → mono-строка. Фон сдержанный: spur-сетка (::before, linear-gradient 88px + radial-маска), glow 0.35 + дуги/кольца за текстом (opacity .8), лёгкий параллакс bg ±30. Слот HAS_HERO_ASSET теперь фоновый (opacity .35). Cover-strip интро и LCP-механика сохранены
- SectionScope переписан (R2.2): грид строки `3ch | name 1fr | desc 1.15fr | plus auto`, описание живёт ПО ЦЕНТРУ строки (в потоке, opacity 0 + y12 → видно при .is-active — высота строки стабильна), плюс справа → × (rotate 45), цифра краснеет. АКТИВНАЯ СТРОКА через document.elementFromPoint: mouseX/Y с mousemove + schedule() на каждый scroll-тик Lenis И window.scroll (rAF-throttle), класс меняется только при смене найденного row; mouseleave документа сбрасывает. Проверено: скролл колесом при НЕПОДВИЖНОЙ мыши переключает active (1→3→1). Появление: clip-path inset(100% 0 0 0)→0 + y44, 0.9s power3.out, stagger 80ms, ST once. Тач (hover:none): первый пункт открыт, тап переключает (desc под заголовком grid-areas). Клавиатура: кнопка-триггер с aria-expanded/controls сохранена
- Билд чистый, обе локали 1440/390 без переполнений

## R2-P1 (лого + glass + ползунки) — ГОТОВ
- ui/LogoOverdone.vue: inline SVG из assets/img/logo-overdone.svg (копия клиентского logo.svg), красный круг в g.logo-overdone__circle (origin 50%/50%, transform-box view-box), hover по родительской ссылке → rotate(-90deg) 450ms cubic-bezier(0.45,0,0.15,1). В шапке (40px) и прелоадере (56px; preloader-spin теперь крутит красный круг −90→0). OdLogo.vue удалён
- _mixins.scss: @mixin glass (бордер strong + rgba(255,255,255,.03) + blur 16 + @supports-fallback surface). Применён: included-chip (radius md), compare-card, calc-card, section-contact__form-wrap, align-card. FAQ и Steps получат glass при своей перестройке (P6); в пинах blur пока не участвует
- PSlider переписан (r2-s3): невидимый НАТИВНЫЙ input поверх кастомных слоёв (track 2px, красный fill, ручка 20px bg+белый бордер → красный+glow на :active/:focus-visible, cursor grab/grabbing), значение над ручкой (клэмп 8–92%), крайние ступени под треком (mono muted; prop ends для continuous), пружинка cubic-bezier(0.34,1.35,0.64,1) 200ms, prop keyStep (calc: 25/20/25 — стрелки по ступеням). Проверено puppeteer: drag 35→82 с bucket-лейблами, ArrowLeft 82→57, UA-ступени с ₴, обе локали без переполнений, билд чистый

## Статус до R2: 7 этапов + самопроверка + R1 готовы (2026-08-26)

## R1 (2026-08-26) — детали в CHANGES_R1.md в корне

Ключевое: настоящий SF Pro Display 400/500 (файлы = прод, md5 сверен; субсет woff2 в assets/fonts, Inter удалён); шкала под живой сайт (h1 96/46, h2 56/36, h3 40/28, все заголовки weight 400, ls −3% везде, mono без uppercase); фикс исчезновения scope-пунктов при hover (reveal() теперь оставляет opacity inline — Vue затирал JS-классы при ре-рендере); калькулятор с непрерывными ползунками 0–100 (bucketLabel для подписей, PSlider props step/value-label); форма в surface-плашке с dashed-полями; футер по прод-структуре + столбец Services (ссылка на эту страницу) + сегмент-свитчер UA|EN; eyebrows только hero+contact; align/faq/included-outro влево; скрыты hero.lead_2, layers.p2, model.p4, contact.p2 (локали не тронуты); SVG-декор добавлен в hero/steps/align/contact. При замерах прода: puppeteer-скрипты в scratchpad (od-inspect.js, od-census.js).

## Самопроверка (полный визуальный аудит через puppeteer-core + headless Chrome)

Панель браузера в сессии заморожена (rAF) — аудит сделан скриптом в scratchpad (audit.js): ~60 скриншотов (1440/768/390 × en/ua, reduce-пасс для лейаута + моушен-пасс: прелоадер, интро, оба пина в середине хода, форма с ошибками/успехом, колода, бургер-меню). Найдено и исправлено:

1. **КРИТИЧЕСКИЙ — scoped CSS miscompile в AppPreloader**: `:global(html.od-preload) .app-preloader { :deep(.od-logo__ring) }` компилировался мусором — spin-анимация кольца применялась к САМОМУ `<html>` (вся страница вращалась −70°→0 при первой загрузке!), а правила оверлея дропались (display:none при armed → прелоадер вообще не показывался). Фикс: НЕскоупленный style-блок с плоскими селекторами. Именно это отравляло LCP: после фикса Lighthouse Perf 70→**94** (A11y 97), LCP 1.8s = hero h1, С прелоадером и интро. НЕ комбинировать :global() с вложенным :deep() нигде
2. **КРИТИЧЕСКИЙ — LangModal focus() скроллил страницу в самый низ**: модалка телепортирована в конец body, plain focus() прокручивает документ к DOM-позиции. Фикс: `focus({ preventScroll: true })` (и в focus-trap). Ловится только с живым rAF — в замороженной панели не проявлялось
3. Мобильная сцена слоёв (≤768): подписи наезжали друг на друга (узкая колонка, ряды absolute с фикс-шагом). Фикс: подписи рядов скрыты, планы по центру, под сценой легенда name+desc (layers-scene__legend)
4. Тикер в reduced-motion не переносился (flex-shrink:0 группы) — обрезался за краем. Фикс: flex-shrink:1 + width:100% + white-space normal в reduce-блоке
5. Карточка партнёра: текстовая лого-заглушка «PAPARAZZI» дублировала h3 «Paparazzi». Фикс: лого = визуальное имя, h3 → .visually-hidden (правило постоянное: с реальным лого то же самое)
6. Подпись графика «Our percentage» задевала красную кривую — сдвинута (y 244→252)

Проверено визуально и ОК: hero+интро (плашки), тикер-маркиза, карточки проектов, изометрия слоёв (desktop статика и pin 25%: мокап наклоняется, слои выезжают; после пина «дыр» нет), сетка 11 чипов, сравнение с бейджем и glow, график, scope-аккордеоны, pin шагов (номер 03→05 красный, активный текст белый, линия прогресса), калькулятор (₴, слайдеры, чипы), колода, FAQ, форма (ошибки красным под полями, success), футер, бургер-меню, прелоадер (лого+спин на 0.99-оверлее), модалка языка, UA-локаль на всех ширинах. Аудит-скрипты: scratchpad/audit.js, recheck.js, pins.js, debug-scroll.js, debug-rotate.js

## Этап 7 (полировка, прелоадер, a11y, перфоманс, SEO, HANDOFF) — ГОТОВ

- **Прелоадер** (`components/layout/AppPreloader.vue`): оверлей в SSR-HTML, показывается ЧИСТО классом `html.od-preload`, который inline-скрипт head (nuxt.config.js) ставит ДО первой отрисовки (пропускает повторные визиты по sessionStorage('od_intro') и reduced-motion). Весь визуальный цикл — CSS-анимации (лого-поп 600ms + выезд кольца → fade-out на 850ms → visibility hidden к ~1.2s), БЕЗ зависимости от гидрации. JS только: открывает гейт hero-интро на 800ms, ставит session-флаг, убирает ноду на 1250ms. ВАЖНО: конфиг head-скрипта — при правках nuxt.config dev-сервер надо перезапускать (авторестарт не сработал)
- **Hero-интро переписано LCP-безопасно** (`coverLineReveal`): H1 никогда не скрывается/пересобирается (любой hide-then-show переносит LCP-кандидатуру на поздний paint + сплит давал TBT 700-900ms от layout thrash). Вместо этого: строки меряются через Range.getClientRects (фрагменты акцент-спанов сливаются по top), поверх кладутся плашки цвета фона, которые схлопываются scaleY→0 (origin top) со stagger 90ms + мягкий settle всего заголовка (y 14→0). Интро играет ТОЛЬКО при активном прелоадере и при performance.now()<1000 (на медленной гидрации hero остаётся статичным — не мигает)
- **Шрифты**: preload Inter-600-latin + Inter-400-latin; metric-compatible фолбэк 'Inter-fallback' (local Arial + size-adjust 107.4% / ascent 90.44% / descent 22.52%) в _fonts.scss и в $font-display — убил font-swap CLS (0.15 → 0)
- **Контраст**: бейдж сравнения → accent-hover (5.2:1 vs 4.4); muted→secondary на surface-подложках (PSlider label, calc title/result-label/disclaimer, compare row label, LangModal note, project number/soon-note, align num). Осталось осознанно: белый на брендовом красном primary-кнопок 4.2:1 (задокументировано в HANDOFF §9-10)
- **SEO**: reactive description = hero.lead (меняется с локалью, проверено), og:title/description/type/site_name, twitter card, favicon с прода. og:image — TODO(ASSET)
- Заглушка партнёра пережата 232KB png → 28KB jpg; лого lazy
- `HANDOFF.md` в корне: перенос, файлы, зависимости, все TODO, добавление партнёра, Lighthouse-методика
- **Lighthouse (mobile, прод, --throttling-method=devtools)**: без прелоадера **88/97**; с прелоадером 70/97 — полноэкранный оверлей лишает hero LCP-кандидатуры (paint под opaque-слоем не регистрируется; 0.99-alpha и 1px-щель не помогают), метрика цепляется за поздний paint во время служебного скролла LH. Остальные метрики зелёные (FCP 1.8s, TBT 90ms, CLS 0). ВАЖНО: simulated-режим (дефолт) на localhost даёт ложный FCP ~4.5s — мерить только devtools-методом
- Финальный проход: heading-иерархия (1×h1, 11×h2, без пропусков), иконочные кнопки все с aria-label, skip-link, img все с alt+размерами, обе локали без переполнений, консоль/сеть чистые, повторный визит без прелоадера/интро

## Этап 6 (SectionFaq + PInput/PChips + SectionContact) — ГОТОВ

- `components/ui/PInput.vue`: прозрачный фон, только нижний бордер (strong → белый на focus → --color-error при ошибке), label сверху (small muted, звёздочка required красная), error 12px / hint 12px под полем, aria-invalid + aria-describedby, фикс -webkit-autofill (inset box-shadow цвета фона + text-fill-color)
- `components/ui/PChips.vue`: мультивыбор пилюлями, aria-pressed, role=group + aria-labelledby, min-height 44px, selected = белый бордер+текст на surface. Осторожно: props обновляются в nextTick — два .click() в одном тике перезапишут друг друга (только в тестах, живые клики ок)
- `components/sections/SectionFaq.vue` (§13): 7 вопросов на PAccordionItem, openIdx — максимум один открыт (открытие нового закрывает прежний), все закрыты по умолчанию, ответы с \n\n разбиваются на абзацы, колонка 840px по центру, reveal stagger
- `components/sections/SectionContact.vue` (§14, id="contact"): слева текст + Telegram (secondary); справа форма. Табы role=tablist/tab/tabpanel + стрелки клавиатуры, подчёркивание-индикатор slide 250ms (translateX 100%). ОБЕ панели всегда в DOM: неактивная absolute+opacity 0+visibility hidden (недоступна табу) — смена fade 200ms без прыжка высоты; min-height панелей = max(scrollHeight обеих), меряется после fonts.ready + на resize (debounce 150ms) + при смене языка (watch lang) — UA даёт 361px против 340 EN. Поля: общие name*/company/contact*(hint); таб A: site* (мягкий URL-чек /^(https?:\/\/)?\S+\.\S{2,}/), selling, PSlider volume (calc.volume_steps — единый список), чипы orders; таб B: sellingPlan*, чипы channels, PSlider volumeExpected. Валидация on-blur + on-submit, только видимый таб + общие; ошибки хранятся КЛЮЧАМИ ('required'|'url') → тексты переводятся живо при смене локали; первый невалидный input получает focus. Submit: loading 800ms mock (console.log payload, // TODO(API): endpoint) → success-состояние вместо формы (title+text+Telegram), высота колонки формы фиксируется перед свапом (min-height). Свечение снизу + TODO(ASSET)-слот
- index.vue: плейсхолдеров больше нет, все секции §0–§15 подключены

### Проверено (этап 6)

Билд чистый. Таб A: пустой submit → 3 ошибки + фокус на первом невалидном, «not a url» → err_url, валидное → loading → success (min-height 814px сохранён). Таб B: индикатор едет, панель A скрыта absolute, валидируется sellingPlan вместо site, success достигается. FAQ: единственный открытый (4-й закрывает 1-й), aria-expanded. Тач-таргеты: таб 48 / чип 44 / submit 52 / faq 71. UA на 390: табы помещаются, чипы/hint на месте, ошибка переводится на лету (Заповніть це поле ↔ This field is required). 768: контакт в 1 колонку. Переполнений нет нигде. Известный артефакт dev-сервера: после удаления style-блока из index.vue висел 404 на старый css-модуль — лечится рестартом dev-сервера

## Этап 5 (SectionSteps + PSlider + SectionCalc + SectionAlign) — ГОТОВ

- `components/ui/PSlider.vue`: переиспользуемый ползунок на нативном input[type=range] (двойной режим: options-массив ступеней → value=index, или числовой min/max). Трек 2px с заливкой через `--fill` inline-var (webkit gradient / -moz-range-progress), ручка 20px белая, красный обод (box-shadow) при :active и :focus-visible, label слева + текущее значение справа (tabular-nums), aria-valuetext строкой ступени, события input/change. Используется в калькуляторе, в этапе 6 — в форме
- `components/sections/SectionSteps.vue` (§10, вторая pin-секция): та же схема, что SectionLayers — дефолтная CSS-вёрстка = статичная вертикальная timeline (линия слева, mono-номера-точки на линии, все шаги раскрыты, desc_2 шага 04 на месте); `gsap.matchMedia('(min-width:1025px)+no-preference)'` включает .is-motion: две колонки, pin +200% scrub 1, слева огромный номер ($fs-number mono, tabular) + текущий заголовок — оба через Vue `<Transition>` (concurrent, absolute) со slide+blur 250ms, входящий номер стартует контурным (-webkit-text-stroke muted, color transparent) и «заливается» красным; справа список описаний (item titles в motion-режиме становятся мелкими muted-подписями), активный item белый, остальные opacity .35 (250ms), тонкая линия прогресса scaleY по scrub + дрейф списка вверх, если выше вьюпорта. activeIdx = floor(progress*5) через onUpdate
- `components/sections/SectionCalc.vue` (§11): слева intro + 9 mono-чипов факторов; 4 связанных (индексы 0,1,2,4) подсвечиваются красным на 900ms при input своего ползунка (pulseFactor + timeout). Справа карточка с glow-бордером: 4 PSlider (margin 4 ст., volume 5 ст., aov 4 ст., integrations 1–10), результат «X–Y%» ($fs-h2, tabular-nums) + suffix; каунтер через gsap.to на plain-объекте с snap:1 и onUpdate→reactive (300ms), reduced-motion — прямое присваивание. Формула `estimateRange()` отдельной функцией с TODO(FAKE)-комментом: база 6; маржа +0/0.7/1.3/2; объём −0/0.5/1/1.5/2; чек −0/0.35/0.7/1; интеграции +0.5…1.5; clamp 2–12; диапазон [round(x−1), round(x+1)] — проверено перебором всех комбо: спред всегда 2 п.п. (дефолт 6–8%, min 3–5%, max 9–11%). Дисклеймер 13px под результатом, outro под сеткой
- `components/sections/SectionAlign.vue` (§12): центр, колода 4 карточек в контейнере aspect-ratio 16/8; слоты 0–3 (front → back: y/scale/rotate ±2deg/opacity), advance: верхняя получает .is-out (вверх+fade 350ms) → setTimeout 350 → current++. Автоплей setInterval 3.5s с паузой на hover и при document.visibilityState hidden; клик по колоде листает вручную и перезапускает каденцию; точки-кнопки (aria-label = текст карточки, aria-current) кликабельны. Reduced-motion: CSS-грид 2×2 (1 колонка ≤480), точки скрыты, JS-автоплей не стартует
- Подключены в index.vue; остался один плейсхолдер — faq

### Проверено (этап 5)

Билд чистый, сеть без 404. Пин шагов: спейсер, диапазон ровно +200%, active 0→2→4 по прогрессу, номер/заголовок обновляются (старые Transition-элементы висят только в замороженной панели — нет transitionend). Калькулятор: слайдеры двигаются (aria-valuetext, --fill), чипы пульсируют и гаснут, формула верифицирована в Node перебором. Колода: клик листает (is-out → ротация слотов), точки синхронны и кликабельны. Мобайл: шаги — статичная timeline без пина (счётчик скрыт, номера на линии, всё раскрыто). Обе локали 390/768/1440 без переполнений, ₴ рендерится (cyrillic-ext). tabular-nums на результате. Каунтер-анимацию и плавность скраба глазами проверяет пользователь (панель скрыта).

## Этап 4 (SectionCompare + SectionModel + SectionScope) — ГОТОВ

- `components/sections/SectionCompare.vue` (§7): два блока-карточки один под другим («Класична розробка» / «Партнерська модель»), по 5 строк «критерий (mono muted) — значение» из compare.rows; второй блок — красный бордер rgba(241,38,37,.4) + glow box-shadow + бейдж «Наша модель» (mono-пилюля, правый верх). Hover-связь: делегирование mouseover/mouseleave на обёртке, data-row индекс → hovered ref → .is-hot на ОБЕИХ строках пары (фон surface-2). Появление парами: timeline с ST once, `tl.from([classicRow_i, partnerRow_i], ...)` с позицией i*0.12, onStart добавляет .is-revealed (совместимость с .reveal pre-hide)
- `components/sections/SectionModel.vue` (§8): слева p1–p4 + p5 body-lg, справа SVG-график 560×380: ось времени, пунктирная отметка «Запуск» (x=210), плоская muted-линия оплаты (24→210, y=314), белая линия продаж (C-кривая до y=92, 2px) и красная линия процента (до y=212, 1.5px) от точки запуска; точки-маркеры и подписи (chart_sales/chart_share) на концах. Отрисовка stroke-dashoffset (getTotalLength → gsap.set) в timeline по ST once top 75%: оплата 0.7s → запуск fade → продажи 1.2s power2.inOut → процент 1.2s (−0.25 внахлёст) → подписи. Reduced-motion: dashoffset вообще не ставится — линии статично видны. ≤1024: график под текстом. Цифр нигде нет
- `components/sections/SectionScope.vue` (§9): 7 строк без карточек (бордеры между), номер 01–07 mono + h3; open-модель: `isOpen(i) = pinned===i || (canHover && hovered===i)`; canHover = matchMedia('(hover:hover) and (pointer:fine)') в onMounted, на тач-девайсах pinned=0 (первый открыт). Hover на li открывает/закрывает, клик по кнопке фиксирует (остаётся после ухода курсора), повторный клик снимает. Высота — grid-rows 0fr→1fr 300ms (тот же трюк, что PAccordionItem), сдвиг строки +8px и красный номер при открытии, aria-expanded/aria-controls/role=region, reveal stagger 60ms
- Все три подключены в index.vue; плейсхолдеры §7–§9 удалены

### Проверено (этап 4)

Билд чистый, сеть без 404. Интерактив: hover партнёрской строки 2 подсвечивает обе строки пары, leave снимает; scope hover открывает (aria true) и закрывает по leave, клик фиксирует и снимает. Мобайл (эмуляция тача): hover-MQ false → первый пункт открыт, тап открывает другие; compare-строки в 1 колонку; график под текстом. Обе локали на 390/768/1440 без переполнений; UA-бейдж не пересекается с заголовком (клиренс 500px). ST-триггеров 13 (пары+график+reveal-ы созданы).

## Этап 3 (SectionLayers + SectionIncluded) — ГОТОВ

- `components/sections/SectionLayers.vue` (§5, флагманская pin-секция). Архитектура: **CSS описывает финальное разложенное состояние** (мокап магазина в перспективе rotateX(55) rotateZ(-42) + 6 слоёв-плоскостей с mono-подписями справа + вертикальные линии-связи) — это же статичный фолбэк. Desktop+motion (`gsap.matchMedia('(min-width: 1025px) and (prefers-reduced-motion: no-preference)')`): pin на +250%, scrub 1, timeline из `gsap.from()` (мокап из плоского+scale 1.06 → наклон; слои из-под мокапа со stagger 0.6 + autoAlpha; линия scaleY; красная линия в конце) — при teardown matchMedia сам откатывает к CSS-состоянию, отдельная мобильная ветка шаблона не нужна (и гидрация не ломается). Фазы текста (p1+p2 / p3 / p4-акцент): в motion-режиме absolute+crossfade 250ms, переключение через onUpdate-пороги (0.34 / 0.82) → ref phase → классы; в статике все абзацы в потоке. `.is-linked` (phase 2 или статика) — красная линия + подсветка бордеров слоёв. Геометрия сцены на CSS-переменных (--plane-w/--layer-gap и т.д., уменьшаются на ≤768); коллапс-офсеты для gsap.from меряются из offsetTop (не дублируются в JS)
- `components/sections/SectionIncluded.vue` (§6): intro 1–3, сетка 11 чипов 4/3/2/1 колонки, hover бордер+красная иконка, reveal stagger 40ms, финальная строка по центру
- `components/ui/IconSystem.vue`: 11 stroke-1.5 иконок одним компонентом (store, crm, card, tag, warehouse, order, truck, bell, chart, gear, link), маппинг к пунктам — ICON_ORDER в SectionIncluded
- **ВАЖНЫЙ ФИКС**: `gsap.registerPlugin(ScrollTrigger)` перенесён из init() на уровень модуля useScrollMotion (client-guard) — onMounted детей срабатывает раньше init() из app.vue, и все scrollTrigger-конфиги компонентов молча игнорировались (reveal-ы этапа 2 играли при маунте, пин не создавался)
- Dev-хук: `window.__ST = ScrollTrigger` (только import.meta.dev) — для ручного `__ST.refresh()/update()` при QA в скрытой панели

### Проверено (этап 3)

Билд чистый. Пин: спейсер 3290px, диапазон = ровно +250%, секция прибита (top 0) весь диапазон, после конца уходит без «дыр». Фазы переключаются 0→1→2+is-linked по прогрессу. Мобильная статика (390): без пина, сцена сверху, весь текст в потоке, красная линия видна, обе локали без переполнений. 768: чипы 2 колонки, сцена уменьшена. 1440: чипы 4 колонки, pinned-режим 100vh, UA-фазы (макс 192px) < min-height 360. Сеть без 404. Скриншоты ниже первого экрана в этой сессии недоступны (панель скрыта, rAF заморожен) — плавность скраба и визуал глазами проверяет пользователь.

## Этап 2 (hero, тикер, проекты) — ГОТОВ

- `components/sections/SectionHero.vue` (§2): eyebrow, H1 с красным акцентом, lead + lead_2 + mono-строка про два месяца, кнопки (primary → scrollTo('#contact'), secondary → t.me/overdoneit). Появление H1 построчно: кастомный line-split (слова → замер offsetTop → строки в overflow-маски → yPercent 110→0, stagger 90ms; accent-спаны клонируются для сохранения scoped-атрибута; после анимации исходный DOM восстанавливается — безопасно для смены языка). Остальные элементы `.js-intro` fade-up. Визуал справа: `HAS_HERO_ASSET = false` — флаг вместо runtime-пробы img (проба засоряла консоль 404); фолбэк = glow + SVG-дуги. Параллакс ±40px scrub. Mobile (≤1024): визуал absolute за текстом opacity 0.5
- `components/sections/SectionTicker.vue` (§3): чистый CSS-marquee (трек = 2 группы, translateX −50%, 30s, пауза на hover) — не зависит от ширины/шрифтов/языка; копия aria-hidden; reduced-motion → статичная строка с переносами (копия скрыта)
- `components/sections/SectionProjects.vue` (§4): массив partners (добавление партнёра = объект), карточка: номер 01 mono, лого, h3, описание, кнопки visit (ghost → url, _blank, @click.stop) / case (disabled + «Кейс — скоро» при caseUrl null); справа «браузерное окно» (бар с 3 точками, aspect 4/3, автоскролл скрина при hover translateY −23% 6s). Карточка целиком кликабельна (article @click → url). Финальная карточка «Ваш продукт…»: пунктирная рамка, большой +, glow, primary → #contact. Проверено с 1/2/3 партнёрами (нумерация и варианты кнопок ОК). Reveal-ы через useScrollMotion().reveal()
- Ассеты: `public/img/partners/paparazzi-placeholder.jpg` (сгенерён qlmanage из SVG; TODO(ASSET) — реальный скрин paparazzi.shop → paparazzi.webp), `paparazzi-logo.svg` (текстовая заглушка, TODO(ASSET))
- `plugins/reduced-motion.client.js`: выставляет useState('od-reduced-motion') ДО маунта (onMounted детей срабатывает раньше init() из app.vue — важно для гардов в компонентах)
- Грабля: статический `src` на несуществующий файл в public/ роняет vite build (UNRESOLVED_IMPORT) — биндить `:src="'...'"` динамически

### Проверено (этап 2)

Билд чистый. Обе локали en/ua на 1440/1024/768/390 — без горизонтального переполнения. Hero виден и анимируется (скриншот), тикер бесшовный (трек 2893px > вьюпорта), карточки/кнопки/reveal-ы проверены по DOM. ВНИМАНИЕ: панель браузера в сессии была скрыта → rAF заморожен, скриншоты нижних секций чёрные — визуальную проверку глазами секций §3–§4 пользователь делает сам.

Nuxt-проект инициализирован прямо в этой папке (корень = корень репо, docs/ и prompts/ не тронуты).

### Что сделано в этапе 1

- Nuxt 4.5.2, SSR, чистый JS. Зависимости: gsap, lenis, sass (+vue, vue-router). Плоская v3-структура через `srcDir: '.'` + `dir: { app: 'app' }` в nuxt.config.js
- SCSS: `assets/scss/_variables.scss` (шрифты, шкала, spacing 1–16, radius, breakpoints, миксин respond), `_theme.scss` (все --color-* из DESIGN §1), `_mixins.scss` (container, section-gap, mono-caption, card, accent-glow, focus-visible), `_fonts.scss` (генерированные @font-face), `main.scss` (reset, типографика, .container/.section/.reveal/.visually-hidden). `_variables` + `_mixins` инжектятся глобально через vite additionalData — в компонентах их не импортировать
- Шрифты локально в assets/fonts: IBM Plex Mono 400 + Inter 400/500/600/700 (latin, latin-ext, cyrillic, cyrillic-ext — cyrillic-ext нужен для ₴). TODO(FONT): Inter — временная замена SF Pro Display, замена в одном месте ($font-display)
- `composables/useLang.js`: useState('od-lang'), t(key) с dot-path (возвращает и строки, и массивы/объекты), setLang, init() читает localStorage('od_lang'), обновляет `<html lang>`. Локали `locales/en.json` + `ua.json` — ВСЕ тексты из CONTENT_SOURCE + CONTENT_ADDITIONS дословно. Eyebrow-ярлыки лежат в локалях с одинаковыми EN-значениями
- `composables/useScrollMotion.js`: init() (Lenis lerp 0.1 + ScrollTrigger, один rAF через gsap.ticker, lenis.on('scroll', ScrollTrigger.update)), reveal(targets, opts) (fade+y24, stagger, top 80%, once), scrollTo(), reducedMotion (useState, реагирует на смену media query — Lenis создаётся/уничтожается)
- UI: PButton (primary/secondary/ghost, стрелка в круге у primary, loading-спиннер, block, no-icon), SectionEyebrow (`○ LABEL`), SectionTitle (парсит `[акцент]` → красный span, tag prop), PAccordionItem (grid-rows 0fr→1fr, плюс→крест 45°, aria), OdLogo (настоящий SVG-знак с прода overdone.it, hover — поворот кольца)
- Layout: PHeader (absolute → fixed при скролле вверх c blur, скрытие при скролле вниз; пилюля-нав на overdone.it; Contact → #contact, Book a call → Calendly; мобильное фуллскрин-меню с ленг-свитчером), PFooter (Social/Menu/Additional, реквизиты OVERDONE LLP OC455139 с прода, UA/EN свитчер, Back to top), LangModal (600ms, focus-trap, Esc, закрытие без выбора = en, запоминается)
- pages/index.vue: шапка + заглушки всех секций с правильными id (hero, ticker, projects, layers, included, compare, model, scope, steps, calc, align, faq, contact) + футер + модалка
- Хедер: обработчик скролла БЕЗ rAF-коалесинга (rAF-версия теряла финальное событие и залипал is-fixed)
- LangModal подписан на hasStoredChoice через watch (onMounted ребёнка срабатывает раньше init() родителя)

### Проверено

`npm run build` — чистый (0 warnings; sass deprecations починены через sass:map). Модалка: показ 1 раз, выбор/закрытие запоминаются. Свитчеры языка (футер+мобильное меню) переключают без перезагрузки, `<html lang>` меняется. Шапка: вниз-скрыта/вверх-fixed/наверху-прозрачна. Бургер: открытие/закрытие, scroll-lock. Шрифты грузятся, сеть без 404, консоль чистая. 1440 и 390 без переполнений.

### Как запускать

- dev: `npm run dev -- --port 3100` (в ~/.claude/launch.json есть конфиг "partnership-dev")
- build: `npm run build`

### Дальше

Все 7 этапов завершены. Следующий шаг — пользователь смотрит страницу глазами (скролл-анимации, каунтер, прелоадер в живом браузере не проверялись — панель была скрыта) и присылает скриншоты правок по схеме prompts/REVISIONS.md (r{N}-s{M} → CHANGES_R{N}.md + PROMPT_R{N}.md).

### Полный список оставшихся TODO (дубль в HANDOFF.md)

- TODO(FONT): SF Pro Display с прода → $font-display в _variables.scss
- TODO(ASSET): hero-3d.webp (+ флаг HAS_HERO_ASSET в SectionHero), реальный скрин paparazzi.webp + лого, 3D-фигура у формы, og:image
- TODO(API): endpoint формы в SectionContact.onSubmit
- TODO(FAKE): формула estimateRange в SectionCalc — согласовать с экономикой
- TODO: caseUrl Paparazzi при появлении кейс-страницы (v2)

### Открытые решения/допущения этапа 1

- Тексты CONTENT_ADDITIONS взяты как есть (пользователь не подтвердил вычитку)
- Подписи графика §8 `model.chart_sales`/`model.chart_share` в спеке без текста — добавлены «Продажі»/«Наш відсоток» (EN: Sales / Our percentage) — согласовать
- git не инициализирован (пользователь не просил)
